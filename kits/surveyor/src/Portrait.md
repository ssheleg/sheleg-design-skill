---
category: Signature
---

The product as a flat still at `--r-card` — never in browser chrome (that seam is
`test-drive`'s), never under a shadow (that light is `showroom`'s), never animated.
Inside a `TintPanel` it takes the nested radius: 24 − 12 = 12.

```tsx
<Portrait src="/dashboard.webp" alt="Visibility dashboard, June" />
```
