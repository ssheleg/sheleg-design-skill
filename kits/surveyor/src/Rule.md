---
category: Foundations
---

A 1px rule in the cool grey `--line` (1.28:1 on white — a hairline, not a mark).
`strong` steps to `--ink-ghost`. A rule divides rows, never tints.

```tsx
<Rule />
```
