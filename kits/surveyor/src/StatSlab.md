---
category: Signature
---

The survey's headline reading: a 40px/600 figure set in `--accent` — the one place
the brand teal may carry characters, 3.54:1 on the field clearing the 3:1 large
floor — over an uppercase 16px/600 label in `--ink`. Wrap rows in `sv-slab-row` +
`sv-slab-row__grid`; the row is a container stepping 1 → 2 by its own width.

```tsx
<StatSlab value="100M+" label="AI answers monthly" />
```
