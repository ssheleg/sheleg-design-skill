---
category: Foundations
---

A 1px rule in `--hairline` (5% ink) or `--hairline-strong` (10%). Hairlines separate
paper from card and rows inside a card; they composite at 1.10–1.21:1, so no state
change ever rides on one.

```tsx
<Rule />
```
