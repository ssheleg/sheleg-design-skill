---
category: Signature
---

The yellow highlighter: `--marker` behind quoted praise, square-edged (radius 0), 4px
of side padding, and ink `#262626` on the composited stripe `#fefacf` is 14.26:1. It quotes — testimonials, a phrase in a letter. It never highlights
interface text, a metric or an error.

```tsx
<p>…and <Marker>doubled my conversion rate in 30 days</Marker>.</p>
```
