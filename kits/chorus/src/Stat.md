---
category: Data
---

The counted figure. 32px/500 with `--track-h2` in `--ink` on paper, and
`--good-on-dark` when it sits inside a `Well` — those are two different tokens
because the mint cannot be brought onto paper at all.

The label is 14px/400 in `--ink-muted` at 5.36:1 on `--bg`; the source line is 12px in
`--ink-ghost`, which is a non-text token used here as furniture beneath a figure that
already carries the meaning.

```tsx
<Stat value="10%" label="Visibility" source="ChatGPT · 30d" />
```
