---
category: Data
---

A figure at `--t-card` (32px) in `--ink`, its label above in the monospace, and
`source` beneath in `--ink-soft` where the number came from somebody else.

The figure is set at weight 400 like everything else: this pack has no bold, and a
stat that reaches for 700 has left it.

```tsx
<Stat value="2.4x" label="Conversion" source="Client-reported, 90 days" />
```
