---
category: Foundations
---

One hairline weight, `--hairline`, which is the same value the dashboard's rail
uses for its right edge. `strong` exists for the one separation that has to read
as structural rather than incidental.

```tsx
<Rule />
```
