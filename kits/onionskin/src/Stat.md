---
category: Data
---

A figure with a `Micro` label above it and, where it came from somewhere, `source`
beneath in `--ink-quiet`. The figure itself is `--font-mono` — on this pack a number is
never set in the sans.

```tsx
<Stat value="18ms" label="p50 retrieval" source="30-day window" />
```
