---
category: Foundations
---

The divider, and in this pack it does the work elevation usually does. `hairline` is
1px of `--rule`, which the reference paints on **7,234** borders; `strong` is
`--rule-strong`.

```tsx
<Rule />
```
