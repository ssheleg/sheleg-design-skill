---
category: Data
---

The figure in `--font-display` at 48px on `--track-display`, its label above in 12px at
`--track-caps` in `--ink-soft`, and `source` beneath it in 14px.

`source` is not decoration: this pack's pages quote work, and a number with no stated
origin is the one thing a quoted transcript cannot support.

```tsx
<Stat value="3,200+" label="Tools connected" source="native APIs and browser" />
```
