---
category: Data
---

The number takes the `t1` step at weight **550** with tabular figures; the label
sits above it in `--ink-soft` at the `b4` step, where the body ramp tracks
`+0.04em` — the widest positive tracking in the system, and what keeps a small
label legible without making it bold.

```tsx
<Stat label="Gross volume" value="$1.2M" sub="Last 30 days" />
```
