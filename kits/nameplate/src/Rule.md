---
category: Foundations
---

`hairline` is `--line-soft`, the page's dominant seam — 37 measured borders. `strong`
is `--line`, the same weight in the cooler grey the plate's edge uses.

Both are 1px. There is no other border width in this pack.

```tsx
<Rule />
<Rule tone="strong" />
```
