---
category: Data
---

A figure with its label above and its provenance below, set solid at 30px in
`--w-medium`: the reference sets every figure with a line height of exactly 1, so
a column of them reads as a rack rather than as paragraphs. `source` is the
honest half — the window or query the number came from — and a figure without one
in this pack is a claim without a receipt.

```tsx
<Stat value="99.5%" label="Identification accuracy" source="30-day rolling, all tiers" />
```
