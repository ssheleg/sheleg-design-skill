---
category: Actions
---

**The primary action is black, and that is the reference's own decision.** The
brand orange measures 3.18:1 on the page and white on it is 3.18:1 — it fails as a
label in both directions — so the hero CTA is near-black at 19.66:1. The one place
the site breaks its own rule is the nav button, white on `#F25533` at 3.43:1.

Radius is `--r-control` 12px, not a pill: this pack rounds and does not capsule.

```tsx
<Button>Start 3-day trial</Button>
<Button variant="secondary">Connect site</Button>
```
