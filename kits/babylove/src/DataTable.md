---
category: Data
---

The header row is the rail label reused — 11px/700 at 0.55px tracking, uppercase,
in `--ink-faint`. A row tints on hover and does not lift.

```tsx
<DataTable columns={[…]} rows={…} />
```
