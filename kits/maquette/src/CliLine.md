---
category: Data
---

`--surface-sunken` on a 1px `--line`, `--radius` 8px, label in `--ink-soft` and
command in `--ink`, both Geist Mono.

It sits under the hero's pills. An enterprise page that still shows a command is
telling an architecture buyer that the thing is real — which is the same job the
model does, in a different register.

```tsx
<CliLine label="Build with CLI" command="curl -fsSL https://…/install.sh | bash" />
```
