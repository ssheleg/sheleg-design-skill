---
category: Signature
---

`--surface-2` on a 1px `--line`, `--radius` 8px, Geist Mono at 14px, a `$`
prompt in `--ink-faint`, a copy control at the right.

**It lives in the first viewport.** This is a project page; the first action is a
command. It goes full-width on mobile and keeps its copy control — it is the last
thing to be compromised.

The control changes its label, not only its colour.

```tsx
<InstallLine command="pip install pymilvus" copied={done} onCopy={copy} />
```
