---
category: Foundations
---

The divider, and in this pack it does the work elevation usually does.
`hairline` is 1px of `--rule`, which the reference paints on 43 elements;
`strong` is the single 2px `--rule-strong` it uses once.

```tsx
<Rule />
<Rule tone="strong" />
```
