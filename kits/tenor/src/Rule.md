---
category: Foundations
---

One weight: `--line`, ink at 16%. `strong` exists for parity with the other kits and
resolves to the same value on paper, because **this pack has no second border weight** —
if a divider is not loud enough, the answer is space or a fill, not a heavier line.

On `[data-surface="dark"]` both tones resolve to the inverted hairline.

```tsx
<Rule />
```
