---
category: Actions
---

`primary` is the ink fill and `secondary` the bordered ghost; the accent fill is a separate class, because this pack allows at most one accent action per view. Nothing scales on hover — buttons change fill and press by one pixel.
