# Board — sheleg-design-skill

The work-list between pipeline runs. Stage 0 reads it and quotes the open count;
stage 10 files every ledger row still `open` or `unresolved` as a row here, with
a board id.

Seeded 2026-08-10 by the `2026-08-10-skill-audit` run. Before that date this file
did not exist, which is why five prior runs' deferrals were only ever visible
inside their own carry-over ledgers.

**Priority** is computed, not asserted: `blast radius × age ÷ cost`. A row that
misleads an agent building a page outranks a row that misleads a human reading
the README.

| id | Row | Source | Priority | Status |
|---|---|---|---|---|
| B-001 | Widen `instrument-console`, `workbench`, `briefing-room` to the four sections (`Components`, `Hero`, `Responsive`, `Signature element`). Blocked on an addressable reference: their `Origin:` lines name a product, not an address, so the sections cannot be sourced without inventing them. Resolve by either (a) obtaining a public reference for each, or (b) re-deriving each pack from a live site and re-issuing it. | `2026-08-10-skill-audit` C-1 | high | **closed 2026-08-20 by decision (c) — they are NOT widened.** The row offered three ways out and the third is the honest one: widening means stating per-component states, an opening viewport and its line ceiling, collapse rules and a signature element, and with no addressable source those thirty-odd values would come from taste wearing the authority of a measurement. All three already declare what they decline; what was missing is that a reader of the pack could not tell the declination was a DECISION. Each `Origin:` now says so in the pack's own words — inline, because the bundle has no `docs/` and the repo's own guard refused a citation that dead-ends for anyone who did not clone. `ADR 0003` is the record, and `validate_contract_declaration` now requires a `core` line to name what it declines (3543 → 3550 checks) |
| B-002 | Retire or replace the three packs whose `Origin:` is not addressable, or record an explicit grandfather clause in an ADR. Standing instruction 5 says "no reference, no pack"; three shipped packs predate it and nothing in the library says so to a reader. | `2026-08-10-skill-audit` | medium | **closed 2026-08-20** — `docs/adr/0003-three-packs-predate-the-reference-rule-and-are-never-widened-by-invention.md`: the three `Origin:` lines name a product rather than an address, the packs predate standing instruction 5, they stay `core`, and an addressable reference is the one thing that would change it. The ADR also states what the exception costs — a consumer of `instrument-console` decides the component states themselves — because an exception recorded without its cost reads as a free one |
| B-003 | ~~Prose contrast claims and worst-stop tables (`cyclorama`) sit outside `validate_stated_ratios`.~~ **Superseded and closed 2026-08-20 — both halves accounted for.** The worst-stop half closed on the B-013 pass: `cyclorama`'s table declares `Worst-stop ratio, on \u0060--ink\u0060` and four of its cells name `--field-2`, which moved 12 claims into the arithmetic. The prose half was re-filed as B-053 with a measured count rather than a description, and **88 of those 140 closed in this pass** by testing one hypothesis — the claim is against the pack's own field — and naming `--bg` only where the arithmetic agreed. Coverage went `353 computed / 150 unguarded` to `448 / 57`. | `2026-08-10-skill-audit` | medium | **closed 2026-08-20** |
| B-004 | **One of three done: `atrium` is widened from its live reference.** Read 2026-08-20 from `functionhealth.com`'s own `/_astro/utilities.*.css` and `/_astro/PageSections.*.css`, and mapped through the reference's own token names (`--fill-04` → `--accent`, `--fill-03` → `--ink`, `--fill-01` → `--bg`). `## Components` is its single `.fh-button` class and four skins with every state; `## Hero` the numbers of the full-viewport media hero; `## Responsive` its **184 media queries counted** — 768px and 1100px carry 140 of them — plus the one place it uses a container query and why; `## Signature element` names the fluted-glass hero that Signature motifs already measured. **The pass also found a value the reference does not ship**: `--accent-tint` was `#F7EEEB`, which appears nowhere in the reference's CSS or HTML, against its `--orange-10p: #F6E9DC` — and the mapping is proved by the neighbour, `--green-10p` being this pack's `--good-tint` to the character. Corrected. Contract split 7 core / 22 widened → **6 / 23**. **`orchard` was attempted 2026-08-20 and NOT widened, deliberately — see B-110.** Its palette verified completely (all 13 token values present in the served document, 10 as hex and 3 as `rgb()`), and its responsive model measured: three fixed frames rather than a fluid scale, the opposite of `atrium`. But `## Components` needs per-component states, and a Framer document serves none: no `clamp()`, no container query, and **the pack's own signature control is not in it**. The contract is all-or-nothing, so widening on three of four would be the exact failure `core` exists to prevent. **Still open: `orchard` (needs a rendered page, not a fetch) and `editorial-luxury` (prowl.chat).** | `2026-08-10-skill-audit` | high | open |
| B-005 | ~~**8 of 21 scenarios carry no recorded result.**~~ **Closed 2026-08-20 — the row's own number was two generations stale.** `test/scenarios.md` now holds **32** `## T*` sections and the file's own one-command check prints `zero unrun`: `python3 -c "import re,pathlib; t=pathlib.Path('test/scenarios.md').read_text(); s=re.split(r'^(## T\\d+[^\\n]*)$', t, flags=re.M); print([s[i] for i in range(1,len(s),2) if not re.search(r'\\*\\*Result\|GREEN', s[i+1])] or 'zero unrun')"`. B-011 had already recorded 0 of 23 on 2026-08-12; this row was never updated after it, which is the same class as everything else closed in this pass — a count restated instead of computed. The scenario harness is not read by `validate.py` by design (`docs/DOCMAP.md` says so in the propagation matrix), so its proof stays a person running the command above. Original row: **8 of 21** scenarios in `test/scenarios.md` carry no recorded result (was 13 of 19). T2, T3, T7, T9, T14 and the new T21 were run in fresh contexts on 2026-08-10 and stamped with the tree they read (`9312a85`). Remaining: T1, T4, T5, T6, T8, T10, T11, T12 — T1 is the expensive one and is coupled to B-006, since widening the description obliges the full trigger set. | `2026-08-10-skill-audit` → `2026-08-10-bundle-self-sufficiency` | medium | **resolved** |
| B-006 | The skill description has room again — the front-matter budget split in 1.11.0 freed it, since `description` is now measured against the spec's 1024 rather than sharing one cap with the bookkeeping keys. It still has no trigger for presentation decks, the motion doctrine, design-sync, or any pack name. Widening it changes discovery, so it needs the full T1 trigger set re-run in fresh contexts. | `2026-08-10-skill-audit` | high | **closed 2026-08-20 for the half that is provable, and the other half is re-filed as B-052.** Measured: 955 of 1024 chars, 69 spare, and `deck` absent — so `briefing-room`, which ships for *investor & board decks, 16:9, a deck rather than a page*, was unreachable by the only phrasing anybody would use. Trigger added (988/1024), and the class is now mechanical: `validate_every_surface_class_is_discoverable` requires every enumerated SURFACE class to appear in the description, watched failing by removing the trigger. The list is enumerated rather than derived from the pack table, because that table's `used for` column is prose about an INDUSTRY far more often than about a surface. **What is NOT done: the T1 trigger set re-run in fresh contexts** — widening a description changes discovery, and that is a measurement, not an edit |
| B-007 | ~~`## Hero` in the pack contract is written for a scrolling marketing page and has no meaning for a standalone product pack.~~ **Closed 2026-08-20, and the row's premise was half wrong.** The standalone reading was not missing from the library — `ledger` had already written it: *"A console's hero is its first viewport after sign-in"*, with a kicker, a title at a console size rather than a display size, the answer before the evidence, and a list of what the screen must not carry. What was missing is that **the skeleton did not carry it**, so every console-pack author re-derived it — which is what the T7 author did. The skeleton now names both readings with a worked example each (`datasheet` marketing, `ledger` console) and states that both owe the same two things. **And nothing had ever read the answer**: `validate_hero_states_its_obligations()` now requires a line ceiling AND what holds it across the 23 packs carrying a `## Hero` heading, and found one live defect of each kind — `atrium` stated no ceiling (widened by this loop the iteration before) and `showroom` stated a ceiling with nothing holding it. Both fixed from their references: `atrium` at two lines in the 540px column, its reference's headline being three words; `showroom` with the answer that is not a max-width at all — a centred column with 58px side padding and `text-wrap: balance`, so its ceiling is a four-word budget. Floor 3619 → 3667. | `2026-08-10-bundle-self-sufficiency` T7 | medium | **closed 2026-08-20** |
| B-008 | ~~A `Contract: core` line declares four named sections and nothing else, so a pack's **other** silences stay undeclared.~~ **Closed 2026-08-20, and the answer is neither of the two the row offered.** A free *"also not specified here"* list is unbounded and uncheckable — every pack would write a different one and nothing could compare them. And four is not the boundary either: two silences are real, and they were counted before the answer was chosen. **Themes** split **10 twin / 13 single / 6 surface-variant** — a second block that is `[data-surface="dark"]` or `[data-state="alarm"]` is not a theme, so *"does this pack have a dark mode"* had three answers depending on how a reader looked. **Rank** is ordered in exactly **one pack of 29** (`tenor`: `--sev-ask` → `--sev-limit` → `--sev-never`); the other 28 were silent, `workbench` among them with its four unordered statuses — the row's own case. So every pack now carries a `Themes:` and a `Rank:` line, **derived from its token layer**, and the gate re-derives both and refuses a declaration that has drifted from the thing it describes. Four plants: each line missing, and each claiming what its own tokens contradict. Floor 3667 → 3783. | T7/T24 fresh-context runs | medium | **closed 2026-08-20** |
| B-009 | ~~The code graph is stale.~~ **Closed 2026-08-20 — refreshed, and it needed no key at all.** It had drifted from 13 commits behind when this row was re-filed to **31** by the time the loop reached it. `graphify update .` — the path whose own help says *no LLM needed* — moved `built_at_commit` from `de09f9e` to `856f12c`, **which is HEAD**, and it did not do what the row feared: every class **grew**. Documents **1797 → 2173**, code **1646 → 2105**, concept 243 → 340, rationale 126 → 155; 3812 → **4773 nodes**, 4185 → 4473 edges, 920 communities. The shrink guard never fired because there was no shrink, and the curated graph was copied to `graphify-out/2026-08-20/` before the run either way. **0 placeholder `Community N` labels** survived — the hub-rename covered the 116 communities whose membership moved. Stage 9's own check passes: all ten god-nodes are documented seams, including the third-largest (`Component Spine`, a rationale node from `kits/atrium/src/Button.md`, named in the CHANGELOG and two specs). **Scope, stated plainly:** `graphify-out/` is gitignored — *machine state, not source* — so *current* means current on this machine, and a clone still builds its own. | T-run 2026-08-13, re-filed 2026-08-20 | medium | **closed 2026-08-20** |
| B-010 | ~~**T23 is written and unrun.**~~ **Closed 2026-08-12** — both branches run in fresh contexts, both green, nine findings reproduced and nine fixes shipped in `1.13.1`. Original row: `scoreboard` against `field-notes` — the two closest packs in the library, sharing warm paper, one orange-red accent and hairline rules — needs both branches executed in fresh contexts. Subagent runs were disabled for the session that shipped the pack, so its result line says "written, not yet run" rather than carrying a verdict. This is the first row filed under the new standing instruction 3. | `v1.13.0` scoreboard run | high | **resolved** |
| B-011 | ~~**scenarios with no recorded result**~~ **Closed 2026-08-12 — 0 of 23 remain.** T4, T8, T10, T11 and T12 run blind against the installed bundle, all green; T1 had a result from 1.12.0 that an earlier count missed. Every scenario in the harness now carries a verdict and a date. T23 is the evidence for prioritising them — one pair of runs on a freshly-audited pack returned nine findings that three green gates had not, four of them wrong numbers inside a shipped token layer. | `v1.13.0` scoreboard run | high | **resolved** |
| B-012 | ~~**Five findings from the T5/T6 runs, unactioned.**~~ **Closed 2026-08-12 in v1.16.0** — all five actioned: instrument-console's table declares its base (palette gate 596 → 603 checks), `--accent-ink` restated at 6.17, the kit's `DataTable` gains the selected state the pack mandates, `workbench` ships its type and spacing tokens and its kit drops to zero raw font-sizes, and `scenarios.md` states that a run against this checkout is not blind. Original row: `workbench` ships no type-size or spacing tokens, so the craft bar's "no ad-hoc font size" cannot be met in it and its kit writes 20 raw `font-size:` declarations — either the clause does not apply to this pack or the token layer is incomplete, and nothing says which. The kit's `DataTable` has no selected state though the pack mandates `--accent-weak` plus a 2px inset. `instrument-console`'s palette table declares no measurement base, so none of its per-token ratios are gate-covered, and it states `--accent-ink` at 6.0:1 where the tokens compute 6.17. And `test/scenarios.md` carries the expected answers beside the prompts, so no run against it can be blind. | T5/T6 runs, 2026-08-12 | medium | **resolved** |
| B-013 | **59% of the library's contrast claims reach no check, and the guard is real work rather than a regex.** Measured 2026-08-12: 121 stated ratios, 71 unguarded. **Closed 2026-08-20 — recounted at 513 claims and split three ways instead of lumped: `348 computed, 15 named a partner this pack cannot pair, 150 unguarded (0 in a table declaring no base, 10 at a gradient stop, 140 prose)`.** The sharpest half was inside the number that looked fine: `_tally["guarded"]` was incremented before the pairs were computed, so **15 claims counted as coverage the arithmetic never ran on** — a claim whose only partner is itself, or whose subject is a composite. `guarded` is gone as a figure and each of the 15 prints by `file:line`. The 27 table claims closed by declaring the base each table's own numbers already resolve against (`atrium`/`router`/`orchard` on `--bg`, `cyclorama` on `--ink` with four cells naming `--field-2`) — every base computed, none invented. A third guard attempt (read the subject off the line above) was written and thrown away: the probe that justified it accepted a match under either comparison mode and so reported 7 resolutions that do not exist, and inside the gate it produced two false failures. Five plants, one per bucket, and the three figures pinned in `test/floors.json`. `python3 test/validate_palette.py` → `OK (1951 checks)`; `--self-test` → 5/5 caught. | T10 run, 2026-08-12 | high | **closed 2026-08-20** |
| B-014 | **Closed 2026-08-20 — the guard exists, in all eight members.** The release job refuses a tag whose commit is not an ancestor of the repository's ACTUAL default branch (read from the API, not hardcoded to `main`, so a fork that renamed it is not told its correct tag is unreachable), and the tag checkout carries `fetch-depth: 0` because ancestry on a shallow clone is not an answer. Watched refusing against a reproduction of the defect — a commit that was never pushed, tagged — and watched passing on this repository's real HEAD. The umbrella requires **both** halves in every member and was watched failing when each was removed in turn. Re-filed 2026-08-20: the reachability half is measured clean and only the mechanical guard remains.** Checked with `for t in $(git tag -l 'v*'); do git merge-base --is-ancestor "$t" origin/main \|\| echo "$t"; done` — **no output**, so all 63 tags are ancestors of `origin/main` and a fresh clone reaches every published release. What is still open is the guard, and it is now cheap to state: the release workflow should refuse a tag whose commit is not an ancestor of `origin/HEAD`. `validate_release_register()` added the neighbouring half this pass — a CHANGELOG release with no tag at all is now reported against a declared list — but it deliberately does not reach for the remote, because `npm test` must work offline. Original row: **A published tag sits ahead of `origin/main`, and a clone cannot reach it.** Original row: **Half-closed 2026-08-12 — `origin/main` now contains the v1.18.0 commit,** because this run pushed the three commits it inherited along with its own; a clone reaches every published tag again. The mechanical half is still open. Original row: **A published tag sits ahead of `origin/main`, and a clone cannot reach it.** Measured 2026-08-12 during the `datasheet` run: `v1.18.0` is tagged (`e3bde7a` → `0c23558`), on `origin`, and served by npm, while `main` was **two commits ahead of `origin/main`** — so the published release lives at a commit a fresh clone does not have, and `origin/main` still reads the `v1.17.0` era. The tag was pushed and the branch was not; the release workflow fires on the tag, so nothing failed loudly. Fix is two commands and the second is the one that gets forgotten. Decide whether the release workflow should refuse a tag whose commit is not an ancestor of `origin/HEAD` — that is a mechanical guard for a class this repository has now hit twice (see the `v1.4.0` ghost, 2026-08-04). | `datasheet` run, stage 0 | high | **closed 2026-08-20** |
| B-015 | ~~**A scratch screenshot is committed at the repository root.**~~ **Closed 2026-08-12 in v1.21.0** — `.tmp-fp-hero.png` removed from the tree and `.gitignore` given the `.tmp*` rule that would have stopped it. The run that closed it generated six screenshots and three JSON measurement dumps of its own, all of them outside the repository in a scratch directory, and committed an explicit path list. Original row: **A scratch screenshot is committed at the repository root.** `.tmp-fp-hero.png`, 646KB, entered history in `0c23558` by a `git add -A` that also swept another run's in-flight brief. Nothing references it; `package.json`'s `files[]` excludes it, so it does not reach npm, but it is in the GitHub tree and in every clone. Remove it, and consider whether `.gitignore` should carry a `.tmp*` rule — the retrospective's 2026-08-04 entry already prescribes committing an explicit path list rather than `-A`, and this is what the prescription costs when it is skipped. | `datasheet` run, stage 0 | low | **resolved** |
| B-016 | ~~A count inside a sentence, where the noun after the number is not a counted noun, reaches `validate_counted_claims()` only if the phrasing happens to match.~~ **Closed 2026-08-20, and it found a live wrong number.** The class was NOT closed by a pattern — three were measured and thrown away. Every numeral with no counted noun after it is **2553 spans** of ordinary prose (*three cards*, *one motion methodology*); a numeral **closing its clause** is 62 and nearly all correct ellipsis (*one gate of four*, *wrong two times out of three*); restricting that to sentences naming a library noun is 69 and mostly the word *one*. What IS precise is the **definite article**: `the <numeral>` closing a clause asserts a known set, and there are **seven** in the whole corpus — three of them library counts, and **one of the three was wrong**: `tenor.md:328` said token names are *"not uniform across the twenty"* at twenty-nine packs. All seven now name their noun, which hands them to the existing check forever, and `validate_a_count_names_its_noun()` refuses the bare form. **Naming the noun immediately exposed two more**: `the nine headings` is false against a thirteen-heading contract (rewritten `nine of the thirteen headings`), and a true SUBSET — the accent role in 27 of 29 — is refused as `twenty-seven packs` because the check reads any `N packs` as a claim about the total, so a subset is written `N of the M packs`. Floor 3783 → 3787. | recount 2026-08-12, re-filed 2026-08-20 | medium | **closed 2026-08-20** |
| B-017 | **`field-notes` renders two provenance states in nearly one colour, and the palette gate structurally cannot see it.** Reproduced 2026-08-12 and again 2026-08-20; the dark theme is worse than the row recorded — `--witness` and `--danger` are **1.2 apart** there (0.4 under deuteranopia), not 2.8. **Closed 2026-08-20, both halves.** The peer set now covers what a pack DECLARES: a state map in its token layer (`[EXTRACTED] → --verify-ink · …`), a distinctness claim, or the pack's own disclosure that two tokens share a hue family — and a distance such a disclosure states is computed, which `validate_stated_ratios` cannot do because that reads contrast ratios and this is an OKLab delta. No hex moved: `Origin: graphify.com` makes these values measurements, so the pack answers in writing instead — rust and red-orange are **one hue family** by measurement, the provenance state is carried by the bracketed word, and `--danger` is separated from `--witness` by form. The false sentence *"kept distinct from `--witness`"* is gone, and the Bans now say the conflation they forbid is **in role**. Four plants including the discriminator — a file-wide *"never by colour alone"* must NOT excuse a hard-floor pair. Floor 1951 → 2087. | T24b run | high | **closed 2026-08-20** |
| B-018 | ~~`field-notes` ships a complete dark theme that no prose documents.~~ **Closed 2026-08-20 for the documentation half; the value decisions are B-112.** All three consequences reproduced. The twin is `tokens/field-notes.css:251-291`, thirty overrides, and the Palette now carries them as a table — including the two the row named: `--surface-2` and `--surface-3` are **the same value** `#262019`, so the deepest step the light table names does not exist in dark (and `--ink`/`--ink-2` collapse the same way, which the row did not catch); and **no `--dawn-*` stop is re-declared**, so `--hero-dawn` still ends `--dawn-7` `#E9ECDF` at 98% into `--bg` at 100% — near-white meeting `#14110E` across two percent, the hard edge the Bans forbid. The Palette now says plainly: **do not ship the hero in dark** until that decision is made. The warm-brown contradiction is recorded in Gotchas where a reader hits it, and **the reference cannot settle it**: `graphify.com` serves no dark theme at all today — no selector of any kind, and none of the thirty values in the 488 KB it returns. The row's fourth claim was imprecise: `SKILL.md` carries no such list; `FIGMA_BRIDGE.md:48` uses `workbench` as an **example**, which now says so and points at the `Themes:` line each pack carries. | T24b run | medium | **closed 2026-08-20** |
| B-019 | ~~Three routing ambiguities in `SKILL.md`.~~ **Closed 2026-08-20 — four of the five held, one did not, and correcting one number exposed two more.** (2) The precedence rule now has its third case: a row naming neither a surface nor a mood is an **audience posture**, which does not arbitrate DENSITY and **wins on MOTION and ornament** — which is exactly where both T24 branches split two-to-one. (4) The word `widened` reached the selection text: it appeared **zero** times before. (5) *"Fetch the kit first"* was scoped — a kit exists for every pack, and for a widened pack the kit and `## Components` must agree while for a core pack the kit is the only answer either gives. **(3) does not hold against the current text**: lines 219-226 give the greenfield path explicitly (*build the page in the pack the brief's register points at*, with `workbench`/`showroom` as the no-signal defaults), so the two sentences do not cancel. **What correcting one count found:** `SKILL.md` said *seven* packs are core where six are, still marked `atrium` `**core contract**` eight iterations after this loop widened it, and said *twenty-two* widened where twenty-three are — the third stale in two consecutive releases. All three corrected, and `validate_table_marks_match_the_contract()` now reads the table against each pack's own `Contract:` line in **both** directions, planted both ways. (1) is real and wider than the row states — see B-114. | both T24 branches | medium | **closed 2026-08-20** |
| B-020 | ~~The pack skeleton still teaches a radius rule that a shipped pack records as a defect.~~ **Closed 2026-08-20, and the row's framing was the thing to fix: they are not competing rules.** Measured across the library — **16 packs state radius arithmetic** — and the two rules govern different layers. **The proportional ramp defines the token set** (`--radius-sm: calc(var(--radius) * 0.6)`); **subtraction adjusts a nested instance** where the padding is known (`calc(var(--radius-control) - 4px)`). `ora` uses both and says which is which; `datasheet`'s 16 − 8 = 8 is subtraction done right; `field-notes`' recorded error was **conflating them** — quoting subtraction to justify a ramp number, so the rule said 0, the example said 7.2 and the token layer said neither. The skeleton now teaches both layers, what each governs, and that failure by name. `field-notes`' correction no longer over-reaches into *"subtraction is wrong"*. And `validate_worked_radius_sums_compute()` refuses a written sum that does not compute, excluding a `*(Corrected …)*` note because that is a record rather than a claim — it caught this run's own cautionary quote in the skeleton, where a reader copies what they see. Floor 3830 → 3833. | `2026-08-10-skill-audit`, re-read 2026-08-20 | medium | **closed 2026-08-20** |
| B-021 | ~~**`showroom`'s status chip fails contrast.**~~ **Closed 2026-08-13 in v1.26.0** — the label is `--ink` at 14.65–14.73:1 and the status colour is the chip's tint plus a 6px dot, corrected in the pack and the kit with the measured numbers (2.03 / 1.54 / 2.65 against the fills the kit actually used). The three tokens are declared `@role non-text:`, and `validate_status_on_field()` now checks the class. Original row: **`showroom`'s status chip fails contrast, and the chip is the pack's own remedy for colour-only status.** Reproduced 2026-08-12 with this repository's palette gate: at the 12px the pack specifies, `--good` `#0fc27b` is **2.33:1** on white, `--warning` `#f5b900` **1.78:1**, `--danger` `#ff5b59` **3.05:1** — all far under AA, and lower again on the ~12% tint the pack puts them on. `showroom.md:115-119` introduces the chip precisely because those hues separate by only 4.9 under deuteranopia, so the remedy's own text is less readable than the problem. There is also no `--good-ink` / `--warning-ink` / `--danger-ink` and no 12%-tint token in the layer, so "that status's ink" and "~12%" must be invented at the keyboard against the craft bar's no-literals rule. Fixing it changes a shipped pack's hexes, which is not a quiet edit. | T25b run, 2026-08-12 | high | closed |
| B-022 | ~~**`showroom` specifies its specimen frame's radius twice.**~~ **Closed 2026-08-20 in the self-contradicting-pack pass.** The kit is the tiebreak — `kits/showroom/src/styles.css:236` sets `.sw-specimen { border-radius: var(--radius-3xl) }` — so the Texture bullet was the wrong half and now reads `--radius-3xl`, with the 4px inheritance spelled out. The class has a sweep: `validate_radius_single_valued()` attributes a radius token to the noun that FOLLOWS it, so the nesting sentence every pack writes reads as one rule per noun rather than three contradictions, and a component with two radii fails. Its plant flips the Components row back to `--radius-2xl` and is watched being refused. The sweep found a second instance while it was being written: `scoreboard`'s radius-arithmetic example called its subject a *card* at `--radius-md` while the Components table and the kit both set the card to `--radius-sm`; the example now names the report surface, which really is `--radius-md`. Original row: **`showroom` specifies its specimen frame's radius twice, differently — and the frame is the pack's signature.** `showroom.md:161-163` (Texture) nests "a `--radius-sm` chip inside a `--radius-lg` row inside a **`--radius-2xl` specimen frame**"; `showroom.md:177` (Components) gives the frame **`--radius-3xl`**. Reproduced: `--radius-2xl` is 16px and `--radius-3xl` is 20px. The same sentence carries a subtraction rule keyed to the outer value, so every inner radius inherits the error. The same chain also requires a "`--radius-lg` row" where the data row is specified with no radius at all, which strands the Loader's "same radius". | T25b run, 2026-08-12 | medium | **resolved** |
| B-023 | ~~**Two `showroom` tokens ship one hex for two jobs.**~~ **Closed 2026-08-20.** Both halves: `--accent-soft`'s comment no longer claims to be the focus ring's colour (`--ring-focus` has derived from `--accent` since 1.30.0, and the comment named the old value for fourteen releases), and the `--line-weak`/`--surface-2` collision is now a stated decision — `@role drawn-on: --bg, --surface` in the token layer plus a Texture bullet saying the well takes no hairline and that `--line` is 1.24:1 against it if one is genuinely needed. The class has a sweep in the palette gate, `validate_line_surface_collision()`: a line token and a surface token resolving to the SAME colour fails unless the line declares which surfaces it is drawn on. Identity rather than a low-contrast threshold, because this library's hairlines are deliberately near-invisible — `showroom`'s is 1.15:1 on `--bg` and correct. It found the unrecorded second instance the moment it ran: `atrium`'s `--line-ink` and `--surface-ink` are both `#2A2B2F`, and `--line-ink` is the ghost-control border while `--surface-ink` is the banner a ghost control most plausibly sits on — declared now, with `--on-ink` (13.47:1) named as the border there. And a third: `maquette`'s `--model-line` IS `--bg`, on purpose, which the prose argued and the layer did not. Two plants: the undeclared collision, and a `drawn-on` list naming a token the pack does not ship, which is the escape hatch's own bypass. Original row: **Two `showroom` tokens ship one hex for two jobs, and one comment documents a colour the code does not use.** Reproduced: `--surface-2` and `--line-weak` are both `#edeff3` (contrast 1.00:1), so a `--line-weak` hairline on a `--surface-2` well — including the Loader's own row rules, since the skeleton fills with `--surface-2` — is invisible, while the pack's Bans assume hairlines divide. Separately `--accent-soft` `#94b9ff` is commented "the focus ring's colour" and `--ring-focus` is `0 0 0 3px rgba(38, 109, 240, 0.35)`, which is `--accent`; `--accent-soft` appears in no Palette table and the pack bans a second accent. | T25b run, 2026-08-12 | medium | **resolved** |
| B-024 | ~~**Three `showroom` motion values contradict the doctrine the same bundle ships.**~~ **Closed 2026-08-20, and the doctrine took two of the three corrections.** The nav's `0.3s` was written as a literal in two places while `--dur-base` held that exact value: both now name the token, and the doctrine says what it meant — *at or under 300 ms* — so a control may sit ON the ceiling with a reason and 301 is over. `--dur-panel` at 500 ms is an **entrance**, and the doctrine now states that an entrance is not UI motion and may run longer when measured; the pack says which rule its token answers to. The `sub-500ms` sentence in `SKILL.md`, `SHELEG_DESIGN.md`, `README.md` and the Cursor rule was the wrong half — it lumped hover with entrance and set a bound that `manpage` (600 ms), `tenor` (640/820 ms), `bulletin`, `scoreboard`, `field-notes` and `roster` (500 ms) all exceed with measured values. `validate_motion_bands()` parses the press band and the ceiling OUT of `MOTION_DOCTRINE.md` rather than repeating them, and it found the new instance named in this row's re-file: `prism` prescribed its CTA press over `--dur-fast` at 200 ms with nothing faster in the layer, so the pack gained `--dur-press: 0.14s`. Three plants. Original row: **Three `showroom` motion values contradict the doctrine the same bundle ships.** Reproduced against the files: the nav transitions over **`0.3s`** (`showroom.md:283-285`) where `MOTION_DOCTRINE.md` states "**UI motion stays under 300 ms**" — 0.3s is not under 300ms, and the pack's stated rationale is that this is "slower than everything else on the page", which is also false against its own `--dur-panel` of 0.5s. `--dur-panel` **0.5s** is then spent on every section reveal (`showroom.md:265-266`) against `SKILL.md`'s "hover/entrance motion stays **sub-500ms**" — legal for a drawer, out of range for an entrance. And the panel curve `cubic-bezier(0.65, 0, 0.35, 1)` is an ease-in-out on an *entering* element, which the doctrine routes to ease-out and for which `--ease-drawer` already exists. | T25b run, 2026-08-12 | medium | **resolved** |
| B-025 | ~~The pack skeleton's Components contract lists classes no pack answers.~~ **Closed 2026-08-20 — it is a contract, and the library's own behaviour decided that.** Counted over the **23** packs carrying the heading: buttons in all 23, cards in 22, inputs and navigation in 21, loaders and empty states in 19. Calling the list a menu would license the gaps instead of closing them, so the skeleton now says **contract**, and says the thing that makes it liveable: **"none, and here is why" is a full answer** — what is not an answer is silence, because a reader cannot tell a pack with no loader from a pack whose author ran out of afternoon. **`atrium`'s four gaps are closed by measurement**, read again off `functionhealth.com`: inputs at the button's own 48px control height with a pill border and an `--ink-3` placeholder; the mobile nav whose fill goes `--accent` → `--ink` with an `::after` strip that cannot disagree mid-transition; empty states as `--ink-2` at 16px/300 centred; and **loaders: none, with zero skeleton, shimmer or spinner rules in either stylesheet**. `validate_component_classes_are_answered()` pins the remaining **9** (class, pack) pairs as a ratchet that may only fall — each fall needs its own reference read, which is why it is a ratchet rather than a sweep. Its plant had to be rewritten twice: removing the bullet's label left the words *skeleton*, *shimmer* and *spinner* standing in its body, so the class still read as answered. | T25a against `pigeonhole` | medium | **closed 2026-08-20** |
| B-026 | ~~`pigeonhole` depends on an exclusion that only prose enforces.~~ **Closed 2026-08-20 — the exclusion is right and it now holds by declaration rather than by accident.** Re-measured with the gate's own metric: the closest of the nine category inks is **4.92 ΔE** at full colour (`--cat-notification-ink` / `--cat-team-ink`) and **1.24 ΔE** under deuteranopia (`--cat-marketing-ink` / `--cat-notification-ink`), against floors of 10 and 8 — the row's 4.42 was slightly off and 1.24 was exact. No arrangement of nine hues clears those, so the exclusion is a property of the problem. The pack now **declares** it where the categories are enumerated, with those numbers, and states the carrier: the chip's label word, already required in Components, plus each ink clearing AA against its own tint. `validate_excluded_sets_are_declared()` holds the enumeration equal to the shipped `--cat-*-ink` tokens **in both directions** — a tenth hue with no row fails, a row with no token fails — and requires the carrier to be stated **in the section that enumerates the set**. That last scoping is the fix its own plant forced: searching the whole file let a chip rule three sections away satisfy a declaration about the hues, and the plant broke two of the phrase's three occurrences while the third kept the check quiet. B-017's widening, which this row asked to wait for, shipped on 2026-08-20. | T25a run | medium | **closed 2026-08-20** |
| B-027 | ~~42 declarations across eight token layers restate a token's channels by hand.~~ **Closed 2026-08-20 — and the row's own instruction was the defect.** Re-measured: **71**, not 42, across **14** packs (`ledger` 18, `editorial-luxury` 13, `showroom` 9) — the number grew with the library and nothing was watching it. **56 of the 71 are custom property DEFINITIONS**, and that is what settles the decision the row asked for. The row said *"the literal must ship first as a fallback"*. For a custom property that is false: `--x: rgb(from …)` **parses everywhere**, wins the cascade, and fails only at substitution as invalid-at-computed-value-time — by which point the literal is gone and every `var(--x)` resolves to `unset`. **23 properties across `ledger` and `showroom` shipped exactly that**, and `showroom`'s was `--ring-focus`, whose own comment named the stake — *"an invisible focus indicator, which is the one degradation this library may not ship"* — while relying on the mechanism that cannot deliver it. A third, `ora`'s `--accent-hover`, had **no fallback at all**. All 24 are inside `@supports (color: rgb(from red r g b))` now, and `validate_relative_colour_is_guarded()` refuses any relative-colour custom property outside such a block. **Migration is not the answer for the remaining 47** — see B-116. | 2026-08-13 sweep, re-measured 2026-08-20 | medium | **closed 2026-08-20** |
| B-028 | **13 `rgba()` literals sit within ΔE 2 of a token without equalling it, and only their author knows which are derivations.** Measured 2026-08-13: `datasheet` 5, `field-notes` 2, `orchard` 2, `editorial-luxury` 1, `pigeonhole` 1, `workbench` 1, `datasheet`'s `rgba(232,232,232,0.1)` the furthest at ΔE 1.78. **Near is not drift**: `rgba(255,255,255,0.8)` beside an off-white `--bg` may be deliberately pure white. Each case is a question — was this meant to be that token? — and answering it wrong changes a shipped colour. Not a sweep; a review, one pack at a time, and the answer belongs in the token layer's comment either way. | `2026-08-13-modern-css-audit` | low | open |
| B-029 | ~~The component library breaks at viewport breakpoints although every component knows its own width.~~ **Closed 2026-08-20 — the migration is finished, and the row was counting the wrong set.** It said *15 remain*; 15 kits carry no container query, but **11 of those carry no width query at all**, so there is nothing in them to migrate. Of the four that do: `datasheet`'s two blocks are marked *SELF for the guides, PAGE for the heading* with the reason; `field-notes`' two are marked PAGE and SELF; `maquette`'s one is SELF; and `pigeonhole`'s is a `:root` token switch, which the guard exempts **by design** because `:root` is inside nobody's container. **Zero unmarked, unexempt viewport queries remain.** What was worth fixing is that the guard read only `*/src/styles.css`: measured today, zero width queries live outside it and zero CSS files fall outside that glob — so the coverage was complete **by accident**. It now reads every `.css`, `.tsx`, `.ts`, `.jsx` and `.js` a kit ships, refuses a corpus of fewer than two files, and is planted with a width query in a `.tsx` template literal. Floor 3863 → 3865. | `2026-08-13` sweep, re-measured 2026-08-20 | medium | **closed 2026-08-20** |
| B-030 | ~~Five typographic primitives the library specifies by hand and the browser now does properly.~~ **Closed 2026-08-20 — re-measured, and three of the five do not hold as stated.** (2) *"0 declare a measure in `ch`"* → **11** files use `ch` today against 12 declaring a px measure. (5) *"fluid spacing 0 of 16, type fluid in 5"* → **6** and **15**. (3) is the sharpest correction: *"`font-display` in 21 files, `size-adjust` in none — `swap` without them is a guaranteed reflow"* counted **string occurrences**, including a token named `--font-display: aeonik` and prose. The real numbers are `font-display` in **three** places — all the **same comment line** in `manpage`'s token layer describing its reference — and **zero `@font-face` blocks anywhere**. Metric overrides have no home in this library because it ships no font loading, and that is a decision rather than a gap. (1) and (4) stand: `text-box-trim`/`text-box-edge` and `@property` are both at zero. The skeleton's `## Type` section now carries all three answers with today's numbers and a support line — `text-box-trim` as a recommendation because Firefox lacks it, `ch` as the rule a px measure stops obeying when the face changes, and font loading as explicitly not this layer. `validate_font_loading_stays_out()` holds that last decision, stripping comments first because a comment is what made the original count wrong. **`@property` is filed** — see B-117. | `2026-08-13` audit, re-measured 2026-08-20 | medium | **closed 2026-08-20** |
| B-031 | **The token layers cannot be read by any tool but a browser.** No `$type`, no `$value`, no DTCG JSON — so Tokens Studio, Style Dictionary and Figma's variable import all need a hand transcription, which is the failure mode `FIGMA_BRIDGE.md` exists to prevent. A generator from `styles/tokens/*.css` to DTCG would make the Figma seam mechanical, and it is the one item on this audit that adds a build step rather than a CSS feature — decide whether that belongs in this repository or in the workbench. | `2026-08-13-modern-css-audit` | low | open |
| B-032 | ~~Two kit breakpoints have a real container answer and are still on the viewport.~~ **Closed 2026-08-20 — the arithmetic was done, and neither had a container answer.** `datasheet`'s instrument grid is already `repeat(auto-fit, minmax(180px, 1fr))`: two columns need 180 + 180 + 1px of border = **361px** of grid width, and below that `auto-fit` drops to one with no query at all. The `@media (max-width: 640px)` was not helping, it was **overriding** that — forcing one column on a 640px phone whose instrument may hold two comfortably. Resolved by **deleting** the block; the container answer turned out to be no query. `blueprint`'s two derive to numbers that give the game away: the tick is `--tick-len` 8px at `--tick-gap` 6px, so its two top arms take 16px and at the field's own `--grid-step` of 32px the marks stop reading as corners only below **~48px**; the column rule is 1px at each edge of a column whose inner padding is `--space-8` 32px, and **this pack states no measure anywhere**, so the only derivable floor is the **64px** of padding itself. Both are far below anything a container reaches — the tell that the 768px breakpoint was never about fitting. A registration mark and an edge rule are **ornament**, and how much ornament a page carries is a page decision: both are marked PAGE with that arithmetic written at the block. **TODO-CONTAINER is now at zero and pinned there**, because an escape with no ceiling becomes the habit it was meant to interrupt. Floor 3981 → 3983. | 1.23.0 deferral | medium | **closed 2026-08-20** |
| B-033 | ~~**`scoreboard`'s token layer still licenses what the pack retracted in 1.13.1.**~~ **Closed 2026-08-20.** The comment on `--accent-hover` carries the retraction instead of the permission: it is the hover step of a mark, it may not carry a link either, and a link is `--ink` with an `--accent` underline. The 4.12:1 measurement stays, because it was never the wrong part. Original row: **`scoreboard`'s token layer still licenses what the pack retracted in 1.13.1, and a shipped comment outranks a correction nobody reads.** `tokens/scoreboard.css:50` carries `--accent-hover: #e03d00; /* 4.12:1 on --bg — the one orange that may carry a link */` while `scoreboard.md:371-375` retracts exactly that sentence: *"[CORRECTION — 1.13.1] No orange in this pack can carry a link."* Reproduced 2026-08-13: both strings are present in the shipped files. `SKILL.md` tells an implementer to copy the token layer verbatim, so the retracted permission is the one that reaches them. Fix the comment, then sweep every other pack for a correction that landed in prose only. | T26b run, 2026-08-13 | high | **resolved** |
| B-034 | ~~**`scoreboard`'s dark band inherits the paper status set.**~~ **Closed 2026-08-13 in v1.26.0** — the `[data-surface="panel"]` block now remaps all four statuses to the on-dark values the pack already prescribed (10.21 / 11.49 / 6.92 / 7.51 against that band, where they had been 3.69 / 3.94 / 2.37 / 2.90). The `--ink-faint` half is closed too: it is not remapped and does not need to be, because the panel's `--ink-soft` is a different value now that the statuses no longer borrow it. Original row: **`scoreboard`'s dark band inherits the paper status set, which its own Bans forbid.** Reproduced 2026-08-13: the `[data-surface="panel"]` block remaps eleven tokens — `--bg`, `--surface`, `--surface-2`, `--surface-3`, `--ink`, `--ink-strong`, `--ink-soft`, `--line-weak`, `--line`, `--action`, `--action-hover`, `--on-action` — and **not** `--good`, `--warn`, `--danger` or `--info`. So `var(--good)` inside the ledger paints **3.69:1** on the dark band where `--good-on-dark` would give **10.21:1**, and `scoreboard.md:348-349` bans precisely that: *"The paper status set on the dark band, or the dark set on paper. They are two sets."* The block already proves it knows the mechanism, because it remaps `--ink-soft` for the same reason. Same block: `--ink-faint` is not remapped and collapses onto the panel's `--ink-soft` (both `#9f9fa9`), so a disabled control is indistinguishable from secondary copy on the pack's hero surface. | T26b run, 2026-08-13 | high | closed |
| B-035 | ~~**Eight `scoreboard` numbers or descriptions disagree with the values beside them.**~~ **Elevation half closed 2026-08-20; the rest is re-filed as B-047.** The elevation comment counted "two soft stacks and a hairline" over four shadows, called `--shadow-card` untinted when `rgba(16, 24, 40, …)` is a blue-black, and left `--shadow-panel` in no prose at all. Both the comment and the Texture bullet now name all four with their layer counts, alphas and jobs, and say the tint is deliberate. `validate_elevation_tokens_named()` is the sweep: every `--shadow-*` / `--*-glow` a pack declares must be named in the pack's prose. It returned **sixteen** unnamed tokens across eleven packs on its first run — `almanac`, `awning`, `briefing-room`, `editorial-luxury`, `maquette`, `notation`, `orchard`, `pigeonhole`, `proscenium`, `router`, `workbench` — all now named, and `proscenium`'s own "Elevation is three shadows" turned out to be four. Original row: **Eight `scoreboard` numbers or descriptions disagree with the values beside them.** Reproduced where numeric: `#00D492` is stated at 1.84:1 *"on white"* and measures **1.94** there — 1.84 is its ratio against the paper, and the sentence names the white metric cards. `--shadow-card` is described as *"untinted"* and ships `rgba(16, 24, 40, …)`, a blue-black; the same comment says *"Both stacks are two layers at 4–5%"* where `--shadow-lift` is 16%. *"Elevation: two soft stacks and a hairline"* counts three where four ship, and `--shadow-panel` appears in no prose. The sand focus ring's only stated ratio is against `--bg` for a token whose role is `--surface-sand` only (15.88 stated, 14.60 on its actual ground). The on-dark status range is given as 1.6–2.6:1 against paper where the five values span 1.33–2.72. None is an accessibility failure; all are the class the pack's own 1.13.1 correction exists to police. | T26b run, 2026-08-13 | medium | **resolved** (rest → B-047) |
| B-036 | ~~**`scoreboard` bans radii above 8px and its signature motif is a pill.**~~ **Two of four halves closed 2026-08-20; the token-less literals are re-filed as B-048.** The radius ban now reads *"Radii above 8px — except `--radius-pill`"*, which the section tick needs and the Components table requires, and says the pill is for the tick and nothing else. The pixel-numeral ban no longer cites `font-smooth: never`, which is not a legal value of the property the pack sets and is not a property any engine implements: it names the declaration the Type section actually ships and states plainly where it has no reach — Firefox on macOS has only `-moz-osx-font-smoothing: grayscale`, and Windows has nothing. Original row: **`scoreboard` bans radii above 8px and its signature motif is a pill; three of its stated behaviours have no token, and one has no working mechanism.** The Bans forbid *"Radii above 8px"* while `--radius-pill: 999px` ships and the section tick requires it — the Texture enumeration omits the pill entirely. The display's 42px and 44px steps and the ledger's 75px/95px label columns exist as prose literals with no token and no media query, against the craft bar's *"tokens, not literals"*. The secondary button is specified as *"`--surface-2` fill at 4% ink, `--ink` at 50%"*, where 4% ink over the paper resolves lighter than the declared `--surface-2` and *"`--ink` at 50%"* measures 3.24:1 on it — below AA for the 10–12px label the same row specifies. And the pixel-numeral requirement cites two different properties (`-webkit-font-smoothing: none` in Type, `font-smooth: never` in the Bans) of which the second is not a legal value of the first and neither disables antialiasing outside WebKit — so a non-negotiable typographic rule has no mechanism on Windows or Firefox. | T26b run, 2026-08-13 | medium | **resolved** (rest → B-048) |
| B-037 | ~~**The retirement trigger for standing instructions is computed from annotations runs forget to update.**~~ **Closed 2026-08-20, with SG-04.** All three registers in `docs/evidence/retro.md` stopped at `v1.26.0` / 2026-08-13 while `1.44.0` shipped — eighteen versions — so the trigger at line 11 (*has not fired in five run stamps*) read every standing instruction as dormant and any of them as retirable on a count that did not exist. The Run stamps table is now complete to `v1.44.0`: twenty-six rows derived from `git log`, the tags and the CHANGELOG, with `Diverged?` left *unrecorded* rather than guessed, exactly as the 2026-08-12 backfill of the `v1.14.1`–`v1.18.0` gap did. `validate_release_register()` fails when a CHANGELOG release at or after `1.5.0` has no row, so the register cannot fall behind silently again; it reads table ROWS only, because the section's own header sentence names the range it covers and the first draft let that sentence stand in for twenty-six rows — the plant deletes one row and was watched being refused. Original row: **The retirement trigger for standing instructions is computed from annotations runs forget to update, so an instruction that fires every release can look dormant.** Reproduced 2026-08-13 while pruning: instruction 2's note reads *last fired 2026-08-12 `e9b753f`*, thirteen run stamps back, and by the letter of *has not fired in five run stamps* it should have retired — while stamps 21, 22 and 23 each read the registry before their tag, which is the instruction firing. The same gap sits on 3, 4, 5 and 8: stamp 23 alone evidences firings none of their notes record. The list is capped at ten and the cap is enforced by an arithmetic whose inputs are the part of the close-out a run does last, so the failure mode is retiring a live instruction rather than keeping a dead one. Two candidate fixes: give each run stamp a column naming the instructions that fired (the trigger then computes from the table rather than from prose), or make the prune's own gate criterion 'every instruction that fired has its note updated in this commit'. The first is mechanical and the second is a habit. | the `a9c497e` prune walk, 2026-08-13 | medium | **resolved** |
| B-038 | ~~**`instrument-console` legislates for four statuses and ships two colours.**~~ **Closed 2026-08-20 by shipping the two it was missing.** The prose has required success, warning, danger and info together since the pack's first release, so `var(--danger)` and `var(--info)` were undefined custom properties — invalid at computed-value time, falling back with no error anywhere — in the pack that legislated them. `--info: var(--accent)` is the signal hue by decision (an infrastructure console's *here is a fact* and its *here is the thing to press* are one blue, and a separate hue would break the pack's one-accent rule; the palette gate reports it as an alias rather than a collision). `--danger: #f4526b` is a **pack decision, not a measurement** — the reference paints no danger state, so there was nothing to measure — chosen to clear AA on every field the pack ships (6.02:1 on `--base`, 5.77 on `--surface-1`, 5.10 on `--surface-3`) and to clear every separation floor against every peer including under simulated protanopia, deuteranopia and tritanopia (worst pair 10.3 against a floor of 8.0). The Bans line now names the set it permits. The pack's row in `SURFACE_COMPOSITION.md`'s status map moved with it, and that map is a checked table now rather than a sentence. Original row: **`instrument-console` legislates for four statuses and ships two colours, and its own waiver says the pair fails the gate.** Reproduced 2026-08-14: `tokens/instrument-console.css` declares `--ok: #46d39a` and `--warn: #e0a030` and **zero** `--danger` / `--error` / `--crit` / `--info` tokens, while the pack's Bans read *"**Success, warning, danger and info** always ship with an icon or a word beside the fill"* and the next line forbids adding one — *"no second color except `--ok`/`--warn` status semantics"*. Its Bans also record *"several of these pairs sit inside a dichromat's confusion line; re-stepping them would invent a colour this pack does not own"*, i.e. a prose waiver where `STYLE_PACK_TEMPLATE.md` rule 3 prescribes deriving the value and marking it derived at the declaration (`maquette` is the worked example). A page whose sections are readings **against a threshold** — the pack's own register — has no legal colour for the breach state. | T29b run, 2026-08-14 | high | **resolved** |
| B-039 | ~~**`instrument-console`'s pressed primary drops its label to 3.06:1.**~~ **Closed 2026-08-20.** The press dims the fill to `--accent-dim` **and swaps the label to `--ink`**, which is 5.62:1 there; `--accent-ink` is 6.17:1 on `--accent` and 3.06:1 on `--accent-dim`, and the Palette row says so on the same line as the warning it sat under. Each ratio is written with its partner on one line, which is what `validate_stated_ratios()` needs in order to check it — the first draft of this fix wrapped a claim across a line break and the gate refused it, correctly. Original row: **`instrument-console`'s pressed primary drops its label to 3.06:1, and the table reasons about every neighbouring value except that one.** Reproduced 2026-08-14 by recomputation from its token layer: `--accent-ink` `#0a0e14` on `--accent-dim` `#1f5fb0` = **3.06:1**, below AA for the label the pack's Micro-interactions prescribe (*"press = 1 shade dimmer (`--accent-dim`), no bounce"*). The pack states the resting pair correctly (6.17:1, verified) and even warns that `--accent-dim` is *"a fill, not a label"* — one row above the rule that makes it one. Fix by pressing with opacity or a border rather than a fill swap, or by giving the pressed state its own label token. | T29b run, 2026-08-14 | high | **resolved** |
| B-040 | ~~**`instrument-console` is one of only two token layers with no `prefers-reduced-motion` branch — and it is the pack that mandates a particle field.**~~ **Closed 2026-08-19** — the promise now has an observable. `validate.py`'s `validate_reduced_motion()` reads every token layer and asserts two things per layer: a layer declaring `--dur-*` or `--ease-*` ships a `@media (prefers-reduced-motion: reduce)` branch, and that branch collapses at least one duration to an instant value (≤1ms). The second half is the point of the row — the only thing that had been reading "degrade to calm" was `sloplint.py:208`, which asserts the STRING `prefers-reduced-motion` occurs in `MOTION_DOCTRINE.md`, so a branch that mentioned the query and collapsed nothing would have satisfied every gate. Durations are matched by VALUE, not by name: `paperclip` spells its durations `--t-micro … --t-hero-copy` and a name-keyed check would have read its branch as empty. Re-counted at the close: **29** token layers, **29** declaring motion, **2** with no branch, **0** now. Both fixed — `editorial-luxury.css` collapses all eight of its durations; `instrument-console.css` collapses its four plus `--stagger`, and because a duration cannot stop a rAF loop, `instrument-console.md`'s `## Motion flavor` now states the still for each of its four motifs (particle field → one static frame, scan/dim spotlight → every section lit, scrubbed instruments → fully drawn, rail → already static). The word *reduced* did not occur in that pack at all, which was the other half of this row. Two plants in `PLANTS` and one CI negative control, each asserting the message and not merely the red: the self-test grew an optional `expect` field because every token layer is also copied into its kit byte for byte, so any edit trips the kit-drift check and a plant would have reported *caught* with this check never having run. Floors 3333/1912/629 → 3391/1912/635 (+58 = 29 × 2, +6 = one new sloplint target × six bans, measured). Residual split out as B-045. Original row: **`instrument-console` is one of only two token layers with no `prefers-reduced-motion` branch — and it is the pack that mandates a particle field.** Reproduced 2026-08-14: `grep -L prefers-reduced-motion styles/tokens/*.css` returns exactly `instrument-console.css` and `editorial-luxury.css`; eighteen of twenty ship one. The word *reduced* does not occur anywhere in `instrument-console.md`, which prescribes a WebGL particle field, a scan/dim spotlight and scrubbed instruments. `MOTION_DOCTRINE.md` §9: *"Shipping an animation without a reduced-motion path is a bug, not a polish item."* | T29b run, 2026-08-14 | high | closed |
| B-041 | ~~**`instrument-console` names its glow twice and bans its own shadow.**~~ **Closed 2026-08-20.** The pack has two glows and says so in all four places now: `--accent-glow` is the focus halo, `--signal-glow` is the composite the one active signal element wears, the Texture section names the token instead of retyping its literal, the Palette table has a row for it, and the Bans permit both. Caught as a class by `validate_elevation_tokens_named()`, which reads `--*-glow` as well as `--shadow-*`: a mandated composite that appears in no prose is a value a reader has to reconstruct from a literal. Original row: **`instrument-console` names its glow twice and bans its own shadow.** Its Palette calls `--accent-glow` *"the only permitted glow"* and its Bans read *"no colored shadows besides `--accent-glow`"* — but `--accent-glow` is a **colour**, and the shadow the pack actually mandates is `--signal-glow`, a two-layer composite in its token layer that the prose never names. Read literally the ban forbids the pack's signature shadow. Compounding it, the Gotchas cap the glow at *"one element per viewport"* while Micro-interactions put it on `:focus-visible` — user-driven, so any keyboard user tabbing while the active signal is on screen puts two in one viewport, and no implementer can keep the budget. | T29b run, 2026-08-14 | medium | **resolved** |
| B-042 | **`instrument-console` contradicts its own `Contract: core` line, and its one site-wide curve collides with the doctrine's scrub rule.** The Contract says the hero, its line ceiling and per-component states are *"yours to decide"*; the pack then states a hero ceiling (*"clamp-scaled (hero ~5.25rem ceiling)"*) and both the button press and the focus-visible treatment. Separately, Motion tokens declare *"the one site-wide curve"* while Signature motifs mandate **scrubbed** SVG instruments, and `MOTION_DOCTRINE.md` §6 requires `ease: 'none'` under scrub — the pack never carves the exception, so an implementer reading it alone lands on the bug the doctrine calls the most common one. Its fastest duration token (0.18s) also sits outside the doctrine's 100–160ms button-press band, so a compliant press needs a literal the craft bar forbids. | T29b run, 2026-08-14 | medium | closed 2026-08-20 |
| B-043 | ~~**`instrument-console`'s token layer is 1.4 KB and the pack spends tokens it does not contain.**~~ **Closed 2026-08-20, and two of its five claims were false.** The size premise is stale: the layer is **4869 bytes, 7th smallest of 29**, and `briefing-room.css` (3222) is *smaller* than it — the pack tripled between 2026-08-14 and today. The focus-ring claim **misreads rule 5**, which governs the fallback order for *derived colour* ("a focus ring above all" is where literal-first matters most), not the existence of a ring token; **20 of 29 packs ship none** and the template never asked them to. What was real and is fixed: `--section` was the library's only raw `vh` (`24vh` on section padding, re-laying out every section when a mobile address bar moves) and is now `24svh` with the decision written, matching the five packs that wrote theirs into a comment; and the pack prescribed `--dur-press` while its layer never defined it — a defect the B-042 commit introduced hours earlier, now caught by `validate_prescribed_tokens_resolve`. The type-scale half is real, library-wide and refiled as B-119. | T29b run, 2026-08-14 | medium | closed 2026-08-20 |
| B-044 | ~~**Every pack that bans a weight or a slant owes a base layer, and none of the twenty ships one.**~~ **Closed 2026-08-20 with a sweep.** `validate_emphasis_base_layer()` reads the ban out of the pack's prose and requires the matching base rule in the same token layer. Three packs were missing one and now ship it: `atrium` (`strong, b { font-weight: var(--weight-semibold) }` — the UA's 700 is a weight no face in that pack has, and inside its 300-weight serif the browser fakes it), `notation` (`var(--w-bold)`, which is 500, because the pack has no bold), and `field-notes`, where the answer is a synthesis rule rather than a weight: Bricolage Grotesque has no italic, so `em, i { font-style: italic; font-synthesis-style: none }` refuses the browser's shear without touching Geist's real italic in the body copy. `awning` and `tenor` already shipped theirs. The plant strips `font-weight` from `tenor`'s rule, and writing it exposed a hole in the first regex: `strong, b.disabled { font-weight: … }` satisfied it, leaving every other `<strong>` on the UA default. Original row: Every pack that bans a weight or a slant owes a base layer, and none of the twenty ships one. `<strong>` renders at 700 and `<em>` renders italic with no stylesheet involved, so the violation is invisible to a grep over the CSS and to review. `tenor` 1.33.0 ships the three-line reset; the other nineteen do not, and each needs its own answer because emphasis resolves differently per pack (value, weight, face). Found by walking computed styles on a running page. | `2026-08-15-tenor-product-surface` | high | **resolved** |
| B-045 | ~~**The reduced-motion gate requires one duration to collapse, not every duration, and it cannot tell a declared exception from silence.**~~ **Closed 2026-08-20.** `validate_every_duration_answers_reduce` asks the strict question — every duration declared outside the branch appears inside it, collapsed or re-declared with a reason — and it verifies the third part the row asked for by name: where the reason promises a component-layer stop, the kit is checked for it. Now **29 layers, 0 silent, 6 kept with a reason, 2 promising a stop, both kept**. The row's own measurement was half stale: the uncollapsed pair was **`atrium` and `prism`**, not roster — `roster` already re-declared `--dur-float-a/b` at their own values inside the branch, which is the very form this check now requires, and `prism` had added `--dur-press: 0.14s` without updating its branch. `atrium`'s four flute durations are re-declared with the reason in the CSS instead of only in prose. `0.01ms` is treated as a collapse, not an exception: it is the idiom for *instant but still fires `animationend`*, and 8 of 11 re-declarations are that. | the B-040 close, 2026-08-19 | medium | closed 2026-08-20 |
| B-046 | ~~**The counted-claims guard cannot see a number hyphenated onto its noun, and `docs/DOCMAP.md` — the document whose own subject is single homes — carried two stale facts because of it.**~~ **Closed 2026-08-19** (SG-03, manifesto M-44). `COUNTED` required a **space** between the number and its noun, so the CI sentence naming the build matrix sat stale while fifteen more kits landed, with `validate_counted_claims()` running green over the same file three lines above it. The mechanism mattered more than the fact: the B-040 run had already measured the hole and routed around it, deleting a hyphenated `twenty-nine releases` from its own prose rather than plant a number the regex could not police — a guard contributors write around has stopped being a guard. The separator is now `[ -]`, captured and back-referenced so a compound is read in one alphabet or the other; the plural is still required for the spaced form only, because a hyphenated count is a modifier and singular by grammar while `one kit per pack` is a ratio, not a tally. Blast radius measured before shipping, old pattern against new on one tree: **34 → 36** spans inside the source list (`+2`, both in `DOCMAP.md`: `thirteen-heading`, already correct, and the kit count, which was the stale one), **333 → 384** across all 1025 text files (`+51`, of which 3 are dropped by the identifier exemption). **Zero false positives inside the policed set.** One class exempted by pattern — a count glued into a longer identifier (`feat/four-packs-v1.9.0`, 2 occurrences, neither in the source list); one class exempted by the source list staying short — hyphenated compounds counting something other than the library (`a six-pack backfill`, `the nine-heading contract`), which no pattern separates from a stale claim. Second fact: `## Shared state` opened with `ungated` — "no lease mechanism is in force in this repo" — while `.claude/agent-sync.json` carried `"gated": true` over nine paths and lease files sat in `.agent-sync/leases/`. Fixed by **deriving instead of restating**: DOCMAP now points at the generated `docs/AGENT_SYNC.md`, and `validate_coordination_claim()` asserts the page carries its generated marker, that its guarded-file list equals the config's, and that DOCMAP does not re-assert the denial while the page records the repo as gated. Three plants added, each asserting the message via `expect`; `--self-test` 19/19. Floors 3391/1912/635 → 3398/1912/635, the +7 attributed by running each function in isolation against this tree and against `7e630cb` (+2 counted spans, +3 coordination checks, +2 relative links). | SG-03, the manifesto conformance program | high | closed |
| B-047 | ~~**The rest of B-035: five `scoreboard` statements whose numbers or subjects still disagree with the values beside them.**~~ **Closed 2026-08-20, and it was two, not five.** All 30 ratio claims in the pack were recomputed: 28 agree to within the repository's 0.1 tolerance, including the composited focus halo at 1.28 against a stated 1.29. The two that disagreed: *(1)* a Gotcha said `#00D492` measures 1.84:1 **on white** — 1.84 is its ratio against the paper `--bg` `#FAF9F5`, and on true white it is **1.94**; *(2)* the ring table's header measured every row against `--bg` while the `--ring-focus-sand` row confines its token to `--surface-sand`, so its stated **15.88** described a pairing that never renders — on sand the same token is **14.60**. Both were fixed the way the row preferred: each figure now names a partner the gate can pair, and the palette run **computes** them — `computed_at_least` raised 454 → 456. The row's quoted coverage figure (*150 unguarded at 22 packs*) had itself gone stale during the same day and is now **57 unguarded (11%) at 17 packs**. | B-035 split, 2026-08-20 | medium | closed 2026-08-20 |
| B-048 | **The rest of B-036: three `scoreboard` behaviours with no token, and one contrast defect.** **Closed 2026-08-20, both halves — and one sub-claim of this row was itself wrong.** The row said 4% ink over the paper *"resolves lighter than the declared `--surface-2`"*; measured, the composite is **darker** (luminance 0.873 against 0.941), which is why it pushes the label DOWN. The contrast half reproduced at **3.16:1** for a 10–12px/600 label, below AA, and the row's 3.24 is the same defect at a different compositing convention. Fixed by replacing two ad-hoc composites with shipped tokens — `--surface-2` fill, `--ink-soft` label — and by **stating the ratio it renders** (4.55:1), because the first fix moved the pair out of every check, the same trap as B-017. The literals half: `--t-display-sm` 42px, `--t-display-md` 44px, `--label-col` 95px, `--label-col-narrow` 75px and `--numeral-col-narrow` 70px now exist as tokens carrying the measured values; the kit's three `width:` literals point at them and the prose names them. New gate `validate_composited_fill_contrast` computes text on a fill a row asks you to composite — in sRGB gamma space, because a first draft composited this module's linear values and got 1.88:1 where the browser renders 3.16. Four plants. | B-035 split, 2026-08-20 | high | **closed 2026-08-20** |
| B-049 | ~~**A duration in a token-layer COMMENT is outside every band check.**~~ **Closed 2026-08-20.** `validate_token_comments_respect_the_bands` reads a token **declaration** whose name or trailing comment names an element `MOTION_DOCTRINE.md` §3 bands — which is where the row's instance lived. **9 subjects across 8 packs, all inside their band.** The 30 durations mentioned inside comment *prose* were extracted and deliberately excluded: every one describes the reference (*"the reference declares `--t-normal: 250ms`"*), quotes the doctrine's own band, or explains a reduced-motion collapse, so a check reading them flags a pack for correctly reporting what it measured. **The adjudication found more than the row did**: `bulletin`'s `--dur-panel` was called *the nav dropdown* in the CSS and *the nav sheet* in the same pack's prose, and §3 bands those differently — 150–250 ms against 200–500 — so one word made 0.4s a violation and the other made it correct. It is a sheet (full-width, `--dropdown-min-h` 420, a `--surface` plane), the naming moved and the value did not: 0.4s is a pack choice, not a measurement, and changing shipped motion on the strength of a word in a comment is the wrong repair. | the motion-band sweep, 2026-08-20 | medium | closed 2026-08-20 |
| B-050 | **A prescribed foreground/background pair is only checked when the pack states a ratio for it.** **Closed 2026-08-20.** `validate_prescribed_pairs()` reads a Components row's resting cell for a fill and a label, applies each state cell's `fill →` / `label →` swaps, and computes `contrast(label, fill)` at every state against the AA floor for the row's type size. 13 rows across 7 packs name both halves and they carry 8 state swaps. **Both defects it found were in the two rows the widened phrasings added**: `maquette`'s leader label is `--ink` on `--model-top` and those are **the same hex** — `#FFF9F4`, 1.00:1, an invisible label, in the pack whose central rule is that type and object are built from one cream; `prism` puts a `$` prompt in `--ink-faint`, **2.31:1**, a token its own layer calls *"furniture only, never text"*. Fixed with shipped values: a new `--on-model` (`#151515`, the field's own colour) at 17.49:1, and `--ink-soft` at 4.70:1. As the row predicted, this could not have caught B-039 — `instrument-console` has no Components table. 16 rows state half a pair; the count prints. Five plants, including the hover-swap path B-039 is made of. | B-039 generalisation, 2026-08-20 | high | **closed 2026-08-20** |
| B-052 | **The description changed and discovery was not re-measured — and the same carrier regression had already recurred.** B-006 took the description from 955 to 988 chars. Walking every tag on 2026-08-20 found what no diff read showed: **`v1.37.5` (`de09f9e`) removed `scrubbed sections` while the description GREW 1009 → 1021**, so the edit was additive by every appearance — and that phrase was the only thing carrying *section by section*, exactly as the RED T1 run of 1.12.0 had established when it restored it. It shipped in **seven releases**, v1.37.5 through v1.45.0, because T1 was never re-run. Restored (1007/1024) and the class is now mechanical: `validate_t1_carriers_survive` pins the twelve phrases T1's tasks depend on, watched failing against the v1.37.5 edit itself. **What remains open is the measurement, and only that**: no string test proves a phrasing routes here rather than to `super-ux` or `copywriting`. The last fresh-context result is 2026-08-11 (`v1.12.0`) and the description has changed six times since; T1's record now says so above its own green. Running it needs a fresh agent context per task, which this session is instructed not to spawn — **it is an operator authorisation, not a missing technique**. | B-006 close, 2026-08-20 | medium | open — measurement only |
| B-051 | ~~**The `Diverged?` column of twenty-six reconstructed run stamps says *unrecorded*, so the retirement trigger counts stamps and not evidence.**~~ **Closed 2026-08-20 the way the row prescribed — not by archaeology.** The count was **34**, not 26 (5 backfilled 2026-08-12, 26 reconstructed 2026-08-20, and 3 written the same day by runs that could have answered and did not), and the preamble is now **recomputed** by `validate_reconstructed_stamp_count` rather than restated. A derivation was tried first and failed honestly: `Diverged?` is not recoverable from the Log, because **0 of the 34** rows have their commit named there. So this session's five runs wrote their own rows — *runs from here on write their own row*, executed — and the newest five now answer, which opens the third retirement trigger for the first time in eighteen releases. `validate_retirement_window` reports OPEN or CLOSED so a run reads the state instead of assuming it, and the trigger's own wording now says an instruction may only be retired when all five newest stamps answer. | SG-04 close-out, 2026-08-20 | medium | closed 2026-08-20 |
| B-053 | ~~**~~140~~ 47 stated ratios sit in prose that names no partner.**~~ **Closed 2026-08-20, and not by editing 46 sentences.** The pairing check those claims needed **already existed and had never run**: 29 lines inside `validate_stated_ratios` sat behind a `continue`, unreachable since the day it was written, reading as a live check. The comment above it explained the bypass honestly — nine lines fail the check and eight are correct writing — so the nine were adjudicated one at a time. **Three** are out of scope by construction (a comparison against a colour the pack does not ship, a bound over a set of surfaces, a rejected candidate) and are now named in `ARGUED`. **Two** were an indented table row misread as prose, because `line.startswith("\|")` does not see a leading space — a classifier bug that under-reported the closable class and over-reported the residue by the same two. **Four** were wrong writing: the figure and its subject had drifted onto different lines, or the named token was the remedy rather than the subject, and both read to a person exactly like a claim. Fixed in `manpage` and `roster` after recomputing each number (all four were right about the arithmetic). Result: **457 computed + 31 verified by the broad pass, 23 unguarded (4%), 6 prose the pass cannot pair** — from 46. The printed line reports the broad pass as its own class, because counting a verified claim as *unguarded* is the same untruth in the other direction. | B-013 residue, 2026-08-20 | medium | closed 2026-08-20 |
| B-054 | **15 claims name a partner and still compute nothing, and each needs a person to say which.** Printed by `file:line` on every palette run as `unpairable`. Three causes, and they need different answers: a wrapped comment leaves the partner alone with the number so the subject is on the previous line (`awning.css:46`, `datasheet.md:439`); the subject is a composite or an alpha the gate cannot resolve to a solid (`ledger.css:33`'s `--muted`); or the claim argues about a colour the pack deliberately does not ship (`prism.md:305` on plain white). Only the first is a wording fix. The second needs the gate to resolve a composite against a known backdrop — real work, and the same class as B-013's discarded attempts. `unresolved_at_most` is pinned at 15 so the set cannot grow while it waits. | B-013 residue, 2026-08-20 | medium | open |
| B-055 | **7 disclosure figures name more than two tokens, so no check can bind them.** The declared-set check computes a stated OKLab distance only when its sentence names exactly two tokens; `pigeonhole`, `paperclip`, `maquette` and `cyclorama` write three statuses and three figures in one sentence, and binding at that granularity gave every figure to every pair — six failures against six correct sentences, watched. The count prints on every run. The fix is editorial and per pack: one pair per sentence. Cheap, and it converts 7 restated numbers into computed ones. | B-017 residue, 2026-08-20 | medium | open |
| B-056 | **A number stated with no qualifier is not read, and `cyclorama` states one.** `_figures()` binds a distance only when the sentence says `apart`, `at full colour` or `under <cvd>`; *"`--warning` and `--danger` by 14.8"* says none of the three, so the figure is skipped rather than guessed — the right call, and it means one correct-looking number in the library is ungated. Either widen the pattern to `by N` (risking every unrelated number in a sentence) or reword the four sites. Prefer the reword: the pattern is the gate's contract with the prose, and loosening it is how the ratio half became unable to fail. | B-017 residue, 2026-08-20 | low | open |
| B-057 | **217 px literals in pack prose that no token in the pack carries, across 25 packs** — against the craft bar's *"tokens, not literals"*. Measured 2026-08-20 with a deliberately crude filter (breakpoint sentences excluded, table rows and fenced blocks excluded, a token's rem value counted in px), so the number is an upper bound and the first work is to sharpen it: `tenor` 44, `paperclip` 43, `pigeonhole` 17, `ora` 16, `proscenium` 16. Many will be legitimate description — a reference measurement in a sentence about the reference — and separating those from a value an implementer must reproduce is the whole task. `scoreboard`'s five were closed by hand under B-048; a sweep needs the classification first, or it becomes 217 arguments about the gate. | B-048 generalisation, 2026-08-20 | medium | open |
| B-058 | **42 of the 44 lines pairing a token with a percentage are outside the composite check, and that is deliberate.** `validate_composited_fill_contrast` reads a row only when it states a composite AND a type size, which is two rows in the library. The other 42 are washes, hairlines, borders, gradient stops and chip fills where nothing says what carries text — and a parser that guessed would repeat the mistake this file records three times. What would close it honestly: the packs stating a text colour where a composited fill carries text, one row at a time, which converts each into a checked claim. The count of composites skipped for want of a size prints on every run. | B-048 residue, 2026-08-20 | low | open |
| B-059 | **16 Components rows state half a pair — a fill with no label, or a label with no fill — and half a pair cannot be checked.** Measured 2026-08-20 by `validate_prescribed_pairs`, which counts them on every run. Three are closable by the library's own `--on-<fill>` convention, which the ratio check already reads that way; the other 13 are rows where the text colour is genuinely unstated (`blueprint`'s transparent secondary, `field-notes`' card fills) or where a second phrasing is in use. Each closes by naming the other half, one row at a time, which converts it into a checked pair. Deriving the label from the fill's name for all 16 would be a guess with a convention's face on it. | B-050 residue, 2026-08-20 | medium | open |
| B-110 | **`orchard` says its candy pill is *"the only orange object on the page"*, and the served page has no orange object at all.** Measured 2026-08-20 against `gutgutgoose.com`: `#FA7241` occurs **only inside SVG `fill=` attributes** — illustration paths and icons — and **never as a `background-color`**, in the CSS or inline. What does paint controls there is `--surface` `#f6ecdc` (41 declarations), `--bg` `#fffef4`, **`--primary` `#78934a` (14)**, its 10% wash `#78934a1a`, and `--surface-ink` `#3a1b13`. The pack says `--cta` is *"the ONE action colour, a fill"*; on this page the action fill is sage. Three readings and the evidence does not choose between them: the pill is hydrated client-side (though 3 `<button>`, 87 `<a>` and 60 `<input>` DO render statically, with inline background colours); the pack read a page this site no longer serves (`/pricing`, `/shop`, `/product` are all 404); or the role was assigned to a colour sampled from artwork. **Do not rewrite the pack on this.** Resolving it needs the page rendered and its computed styles read — which is also what `## Components` needs, so the two close together. | B-004 orchard attempt, 2026-08-20 | high | open |
| B-111 | **Three warm surface variants the reference ships and `orchard` does not carry.** `#f9eedc`, `#f7ecdc` and `#f2e9df` each appear as a `background-color` in `gutgutgoose.com`'s served CSS, between the pack's `--surface` `#F6ECDC` and `--surface-2` `#FBF7EA`. Either the reference has a warmer step the pack collapsed, or these are one-off section fills that no token should carry — and the pack currently says nothing either way. Cheap to settle in the same rendered pass as B-110, and it is a Palette question rather than a Components one. | B-004 orchard attempt, 2026-08-20 | low | open |
| B-112 | **`field-notes`' dark theme needs three value decisions the reference can no longer settle.** (1) The gradient's tail: no `--dawn-*` is re-declared in dark, so `--hero-dawn` runs `#E9ECDF` at 98% into `#14110E` at 100%. (2) The family: Gotchas says the forest palette was kept and the layer ships the warm-brown `#14110E`. (3) The collapsed steps: `--surface-2` = `--surface-3` and `--ink` = `--ink-2` in dark, where light distinguishes both pairs. **Measured 2026-08-20: `graphify.com` serves no dark theme at all** — no `[data-theme="dark"]`, no `.dark`, no `prefers-color-scheme`, and none of the thirty dark values in 488 KB. So either the reference dropped its dark mode after the 2026-08-04 read, or the block was never read off it, and the evidence does not choose. Re-stepping thirty values from taste is what the pack layer exists to prevent; the Palette says *do not ship the hero in dark* until this is settled. | B-018 residue, 2026-08-20 | high | open |
| B-113 | **Ten of the twenty-nine packs ship a dark twin and three document nothing about it in their Palette** — `daylight`, `notation`, `vitrine`. Measured 2026-08-20 by reading each pack's `Themes:` declaration against its own Palette section. `field-notes` was the fourth and closed under B-018 by writing the twin out as a table. The same treatment fits the other three, and the durable version is a check: a pack declaring a twin must document it, which is one more derived-not-asserted guard of the kind added today. Filed rather than swept, because writing three tables from three token layers is three separate readings. | B-018 generalisation, 2026-08-20 | medium | open |
| B-114 | ~~The motion ceiling has two homes and they disagree.~~ **Closed 2026-08-20 — one home, and the check caught a contradiction this same run created.** The `## Register` is the home, because it is what a reader of one pack has. **Eleven Registers gained a statement**, each relocated from `MOTION_DOCTRINE.md` with the doctrine's own reason (`workbench`/`briefing-room` 3 as not cinematic, `ledger` 3 with its three state loops, `pigeonhole` 4 at the family's floor, `roster` 4 keeping only its sticky nav, `field-notes` **none** as the one standalone exception) or, for the six the doctrine never named, from the default it does state — `MOTION_INTENSITY` is cut by §1's frequency table alone. All **15** standalone packs now state a ceiling or state they have none. `validate_motion_ceiling_has_one_home()` asks three things and **two of its own defects were found by its own plants**: reading the number before the *no ceiling* phrase made a contradiction read as a valid ceiling, and the doctrine writes both *above 4* and *at 4* so four packs were never compared. Corrected, it immediately failed on `proscenium` — where this run's own default sentence sat on top of a Register that already named **4**. Floor 3795 → 3830. | B-019 finding (1), 2026-08-20 | high | **closed 2026-08-20** |
| B-115 | **Nine component classes are unanswered across four packs, and each needs its own reference read.** Measured 2026-08-20 and pinned in `test/floors.json` as `components_unanswered_at_most: 9`: `showroom`/cards, `roster`/inputs, `blueprint`/navigation, and loaders **and** empty states in `manpage`, `pigeonhole` and `roster`. `atrium`'s four were closed the same day by re-reading its reference, which is the shape each of these takes — including the possibility that the honest answer is **none**, as `atrium`'s loader entry turned out to be. Inventing an answer is what the pack layer exists to prevent, so this is nine small measurement passes rather than one sweep. The ratchet holds the number down while they are done. | B-025 residue, 2026-08-20 | medium | open |
| B-116 | **47 restated channel literals do not say what they are derived from, so drift is invisible.** Measured 2026-08-20: 71 `rgba()` literals across 14 packs are ΔE < 0.05 from a token in their own file; 24 are now guarded relative-colour declarations and **47 remain hand-restated**. Migrating them is the wrong fix — 56 of the 71 are custom property definitions, and relative colour there needs an `@supports` block per pack, which is machinery in the file a reader is told to copy verbatim. The right fix is cheaper: each literal states its source (`/* = --on-ink at 72% */`, which `ledger`'s eighteen now do) and a check recomputes the declared relationship, so a token that moves fails in CI instead of drifting silently. 47 comments, derived not chosen, then one check. | B-027 residue, 2026-08-20 | medium | open |
| B-117 | **No token in the library is typed: `@property` is at zero across 29 packs.** Measured 2026-08-20. A typed custom property validates its own value — an invalid one becomes the declared `initial-value` instead of an inherited surprise — and is animatable, which is what a colour or a length token usually wants. The cost is the reason it is filed rather than done: the token layers are files a reader is told to copy verbatim, and a `@property` block per token would roughly double them. The narrow version worth pricing first is the handful of tokens that are actually animated (the accent, the elevation, the durations) rather than all of them — and that list has to be measured from the kits' transitions, not guessed. | B-030 residue, 2026-08-20 | low | open |
| B-118 | **Four packs declare one site-wide curve, mandate scrubbed motion and carve no `ease: none` exception.** Measured 2026-08-20 while closing B-042, which named one pack: the shape holds for **five** — `blueprint`, `maquette`, `scoreboard`, `showroom` and `instrument-console`, the last now carved. `MOTION_DOCTRINE.md` §6 settles the contradiction (*under `scrub`, easing must be `none` — the scrollbar is already the clock*), and easing a scrubbed timeline eases against the scroll position twice, which the doctrine names as the family's most common motion bug. Not one sweep: each carve is written against that pack's own motion section and its own signature motif, so `validate_scrub_carves_its_exception` holds the number at `scrub_uncarved_at_most: 4` while they are done one at a time. | B-042 residue, 2026-08-20 | medium | open |
| B-119 | **12 packs give an implementer no font size to read — no type-scale token and fewer than three sizes in `## Type`.** Measured 2026-08-20 while closing B-043: **6 of 29** packs ship a type-scale token, **11** state three or more sizes in `## Type` prose, and **12 do neither** — `almanac`, `bulletin`, `daylight`, `maquette` and `vitrine` state **zero**. `SKILL.md:194` is unambiguous: *"tokens, not literals; no ad-hoc hex, radius or font size anywhere in the diff"*, and the line under it puts the visual decision in the token layer. An implementer of one of those twelve has to invent every size, which is the one thing the pack layer exists to prevent. Not one sweep and not a token per pack either: prose that states the sizes satisfies the bar as well as a token does, and which of the two is right depends on whether the pack's reference can still be read. Twelve small passes, each ending in three or more stated sizes. | B-043 residue, 2026-08-20 | medium | open |
| B-120 | **A gate constant can be shadowed by a later one of the same name, and Python reports nothing.** Found 2026-08-20 by being committed: a new `DUR_DECL` near line 3120 of `test/validate.py` was overwritten by an existing one at 3716 whose capture groups mean something else, so the new check parsed every duration wrongly and printed `0 kept` against a tree where six were measured by hand — a number, not a failure. `validate_gate_has_no_shadowed_names` walks the three gate modules with `ast` and refuses any top-level name bound twice; the pre-existing duplicate it found (`CSS_COMMENT`, twice, identically) is deduped. **The row stays open for the other three ratchet files**: `validate_palette.py` and `sloplint.py` are covered by the same check, but `test/*_test.js` and the umbrella's `test/` are not, and the same collision is available there. | B-045 close, 2026-08-20 | medium | open |
| B-121 | **58 palette-table rows name a surface other than the base their header declares, and the difference between the ordinary case and a wrong number is not mechanically decidable.** Measured 2026-08-20 while closing B-047, and filed rather than gated *because the broad check was tried and rejected*: in nearly all 58 the row's SUBJECT is a surface token, correctly measured on the declared base, and a gate that flags those teaches an agent to route around it. Only a row that **confines** its token to another surface is wrong, and `validate_confined_tokens_measured_where_used` catches exactly that — with **zero** live subjects after the scoreboard fix, so it ships as a regression guard with a plant rather than as a measurement. What stays open is the other direction: a row whose confinement is stated in prose ELSEWHERE in the pack rather than in the cell, which no pattern in the cell can see. The cheap partial fix is a `Measured on` column wherever a table's rows do not share one base — 47 base-declaring tables exist and one has it. | B-047 close, 2026-08-20 | low | open |
| B-122 | **Nothing checks that a pack's two files agree about what a token drives.** Found 2026-08-20 while closing B-049: `bulletin` called one token a *dropdown* in `tokens/bulletin.css` and a *nav sheet* in `bulletin.md`, and because `MOTION_DOCTRINE.md` §3 bands dropdowns at 150–250 ms and sheets at 200–500, the pack's legality depended on which file a reader opened. The band sweep now reports which pairing it used, so a wrong pairing is arguable rather than invisible — but it still trusts the CSS comment, and a pack that disagrees with itself defeats any band gate by construction. The checkable shape is narrow and worth pricing: for each duration token, collect the element words in the CSS comment and in the prose sentence naming the same token, and refuse a pair that falls in two different bands. The corpus is 112 duration declarations. | B-049 close, 2026-08-20 | medium | open |
| B-123 | **Thirty-four run stamps will never carry a `Diverged?` answer, and nothing marks the boundary where the record becomes trustworthy.** Measured 2026-08-20 while closing B-051: the answer is not derivable (0 of 34 rows have their commit named in the Log), so those rows are permanently blind and archaeology would be invention. What is missing is cheap and not yet done: a single dated line in the table marking where self-written stamps begin, so a reader — and any future trigger that reads a window wider than five — can see the boundary instead of inferring it from which rows happen to say *unrecorded*. The related open question is whether the third trigger should read a window of five at all when 34 of 57 rows cannot answer; five was chosen when the table was short, not measured. | B-051 close, 2026-08-20 | low | open |
| B-124 | **Six prose ratios remain that the broad pass cannot pair, and each needs a person.** Measured 2026-08-20 at the B-053 close: after the pass went live, 23 claims are unguarded — 2 in a table declaring no base (closable by one header edit), 15 placing the figure at a gradient stop (out of scope by construction, and `awning` and `cyclorama` write them correctly), and **6 in prose the pass narrows to a token and still cannot pair**. Those six are the real residue: each is a sentence whose subject the pack does not ship as a solid, or whose partner is a composite. They are individually small and none is wrong — the gate says so — but an unguarded correct number is one edit from an unguarded wrong one. | B-053 close, 2026-08-20 | low | open |
