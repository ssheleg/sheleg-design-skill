# Board — sheleg-design-skill

The work-list between pipeline runs. Stage 0 reads it and quotes the open count;
stage 10 files every ledger row still `open` or `unresolved` as a row here, with
a board id.

Seeded 2026-08-10 by the `2026-08-10-skill-audit` run. Before that date this file
did not exist, which is why five prior runs' deferrals were only ever visible
inside their own carry-over ledgers.

**Priority** is computed, not asserted: `blast radius × age ÷ cost`. A row that
misleads an agent building a page outranks a row that misleads a human reading
the README.

| id | Row | Source | Priority | Status |
|---|---|---|---|---|
| B-001 | Widen `instrument-console`, `workbench`, `briefing-room` to the four sections (`Components`, `Hero`, `Responsive`, `Signature element`). Blocked on an addressable reference: their `Origin:` lines name a product, not an address, so the sections cannot be sourced without inventing them. Resolve by either (a) obtaining a public reference for each, or (b) re-deriving each pack from a live site and re-issuing it. | `2026-08-10-skill-audit` C-1 | high | **closed 2026-08-20 by decision (c) — they are NOT widened.** The row offered three ways out and the third is the honest one: widening means stating per-component states, an opening viewport and its line ceiling, collapse rules and a signature element, and with no addressable source those thirty-odd values would come from taste wearing the authority of a measurement. All three already declare what they decline; what was missing is that a reader of the pack could not tell the declination was a DECISION. Each `Origin:` now says so in the pack's own words — inline, because the bundle has no `docs/` and the repo's own guard refused a citation that dead-ends for anyone who did not clone. `ADR 0003` is the record, and `validate_contract_declaration` now requires a `core` line to name what it declines (3543 → 3550 checks) |
| B-002 | Retire or replace the three packs whose `Origin:` is not addressable, or record an explicit grandfather clause in an ADR. Standing instruction 5 says "no reference, no pack"; three shipped packs predate it and nothing in the library says so to a reader. | `2026-08-10-skill-audit` | medium | **closed 2026-08-20** — `docs/adr/0003-three-packs-predate-the-reference-rule-and-are-never-widened-by-invention.md`: the three `Origin:` lines name a product rather than an address, the packs predate standing instruction 5, they stay `core`, and an addressable reference is the one thing that would change it. The ADR also states what the exception costs — a consumer of `instrument-console` decides the component states themselves — because an exception recorded without its cost reads as a free one |
| B-003 | Prose contrast claims and worst-stop tables (`cyclorama`) sit outside `validate_stated_ratios`, which only checks a claim whose base the document declares. Fix by making those tables declare a base, then widening the check. | `2026-08-10-skill-audit` | medium | open |
| B-004 | Widen `atrium`, `orchard`, `editorial-luxury` to the four sections. Authorised by the operator; their references are addressable (functionhealth.com, gutgutgoose.com, prowl.chat), so this is a measurement pass against live computed styles rather than an editing pass. The misleading half is already closed — each declares `Contract: core` and says what it leaves undecided. | `2026-08-10-skill-audit` | high | open |
| B-005 | ~~**8 of 21 scenarios carry no recorded result.**~~ **Closed 2026-08-20 — the row's own number was two generations stale.** `test/scenarios.md` now holds **32** `## T*` sections and the file's own one-command check prints `zero unrun`: `python3 -c "import re,pathlib; t=pathlib.Path('test/scenarios.md').read_text(); s=re.split(r'^(## T\\d+[^\\n]*)$', t, flags=re.M); print([s[i] for i in range(1,len(s),2) if not re.search(r'\\*\\*Result|GREEN', s[i+1])] or 'zero unrun')"`. B-011 had already recorded 0 of 23 on 2026-08-12; this row was never updated after it, which is the same class as everything else closed in this pass — a count restated instead of computed. The scenario harness is not read by `validate.py` by design (`docs/DOCMAP.md` says so in the propagation matrix), so its proof stays a person running the command above. Original row: **8 of 21** scenarios in `test/scenarios.md` carry no recorded result (was 13 of 19). T2, T3, T7, T9, T14 and the new T21 were run in fresh contexts on 2026-08-10 and stamped with the tree they read (`9312a85`). Remaining: T1, T4, T5, T6, T8, T10, T11, T12 — T1 is the expensive one and is coupled to B-006, since widening the description obliges the full trigger set. | `2026-08-10-skill-audit` → `2026-08-10-bundle-self-sufficiency` | medium | **resolved** |
| B-006 | The skill description has room again — the front-matter budget split in 1.11.0 freed it, since `description` is now measured against the spec's 1024 rather than sharing one cap with the bookkeeping keys. It still has no trigger for presentation decks, the motion doctrine, design-sync, or any pack name. Widening it changes discovery, so it needs the full T1 trigger set re-run in fresh contexts. | `2026-08-10-skill-audit` | high | **closed 2026-08-20 for the half that is provable, and the other half is re-filed as B-052.** Measured: 955 of 1024 chars, 69 spare, and `deck` absent — so `briefing-room`, which ships for *investor & board decks, 16:9, a deck rather than a page*, was unreachable by the only phrasing anybody would use. Trigger added (988/1024), and the class is now mechanical: `validate_every_surface_class_is_discoverable` requires every enumerated SURFACE class to appear in the description, watched failing by removing the trigger. The list is enumerated rather than derived from the pack table, because that table's `used for` column is prose about an INDUSTRY far more often than about a surface. **What is NOT done: the T1 trigger set re-run in fresh contexts** — widening a description changes discovery, and that is a measurement, not an edit |
| B-007 | `## Hero` in the pack contract is written for a scrolling marketing page — "what the first viewport must contain", "the display headline's line ceiling" — and has no meaning for a standalone product pack. Both packs that could demonstrate the standalone reading (`workbench`, `briefing-room`) are `core` and skip it, so an author of a console pack reinterprets the heading alone. Found by the T7 fresh-context author, which did exactly that and said so. | `2026-08-10-bundle-self-sufficiency` T7 | medium | open |
| B-008 | A `Contract: core` line declares four named sections and nothing else, so a pack's **other** silences stay undeclared. `workbench` has four status roles and no rank ramp; a fresh-context agent building an incident list had to infer a severity scale from role descriptions and only discovered the gap by hitting it. Decide whether the contract should carry a short "also not specified here" list, or whether four is the right boundary and the rest is the reader's. | `2026-08-10-bundle-self-sufficiency` T21 | medium | open |
| B-009 | **Re-filed 2026-08-20: the semantic half EXISTS and the row's numbers were wrong.** Measured from `graphify-out/graph.json`: **3812 nodes** and 4185 links, of which **1797 are `file_type: document`** — plus 1646 code, 243 concept and 126 rationale. So the 139 documents this row said carry no semantic nodes are extracted. What remains is staleness alone: `built_at_commit` is `de09f9e` (v1.37.5), **13 commits behind HEAD** by `git rev-list --count de09f9e..HEAD`. That is a re-run, not a credential question and not a modelling decision, and the shrink-guard argument in the original row is the thing to respect when it happens. Original row:  Original row: **Refreshed 2026-08-13 to `a9c497e` code-only; the semantic half is what remains, and it is NOT a credential blocker — writing that down first was this run repeating the mistake the project has already named.** `graphify . --update` reproduced the exact shape instruction 9 exists for: **exit 0**, `error: no LLM API key found (139 doc/paper/image file(s) need semantic extraction)`, and `built_at_commit` unmoved at `9312a85` — 46 commits stale by then, with `links` empty. `--update --code-only` (local AST, no key) then wrote 2059 nodes, 3021 links and 144 communities at `built_at_commit` `a9c497e`, matching HEAD, and the curated graph was backed up to `2026-08-13/` first. Hub check against the docs: the ten god-nodes are all documented seams — `SKILL.md`, the scenarios harness, the gates' `check()`/`read()`/`main()`, the packs — so no undocumented seam and no denied edge. What is still open is the doc side: 139 markdown files carry no semantic nodes. The bare CLI's *"Set GEMINI_API_KEY … ANTHROPIC_API_KEY …"* is the refusal of **one entry point**, not of the operation — the skill states it reads no Anthropic or OpenAI key at all, and with no Gemini key the host agent itself is the LLM. That is the same misreading the 2026-08-10 run recorded as `a-recorded-blocker-outlives-the-path-that-produced-it`, and it built the doc half with no key that day (1703 nodes, 113 communities). This run did not: the no-key path dispatches subagents, which needs the operator's go-ahead, and extracting 139 documents inline is not a release close-out's work. So the row stays open on **scheduling**, not on a secret. Original row: The code graph is two commits behind (`built_at_commit` `9312a85`, 1703 nodes). An incremental re-extraction on 2026-08-10 returned 1652 and the shrink-guard refused it; investigation showed only 52 of 168 lost nodes were `.cursor`-mirror duplicates, the rest real content modelled more coarsely by a different extraction pass — including a node per historical release in `CHANGELOG.md`. Two candidate fixes, and the choice is the work: (a) a full rebuild so one granularity model applies to all 235 documents, which the content-keyed cache makes cheaper than the first build but not free; (b) decide the mirror-dedup model is correct, state it in the extraction prompt so re-runs are deterministic, then rebuild. Do not force past the guard — it was right. | `2026-08-10-bundle-self-sufficiency` | medium | open |
| B-010 | ~~**T23 is written and unrun.**~~ **Closed 2026-08-12** — both branches run in fresh contexts, both green, nine findings reproduced and nine fixes shipped in `1.13.1`. Original row: `scoreboard` against `field-notes` — the two closest packs in the library, sharing warm paper, one orange-red accent and hairline rules — needs both branches executed in fresh contexts. Subagent runs were disabled for the session that shipped the pack, so its result line says "written, not yet run" rather than carrying a verdict. This is the first row filed under the new standing instruction 3. | `v1.13.0` scoreboard run | high | **resolved** |
| B-011 | ~~**scenarios with no recorded result**~~ **Closed 2026-08-12 — 0 of 23 remain.** T4, T8, T10, T11 and T12 run blind against the installed bundle, all green; T1 had a result from 1.12.0 that an earlier count missed. Every scenario in the harness now carries a verdict and a date. T23 is the evidence for prioritising them — one pair of runs on a freshly-audited pack returned nine findings that three green gates had not, four of them wrong numbers inside a shipped token layer. | `v1.13.0` scoreboard run | high | **resolved** |
| B-012 | ~~**Five findings from the T5/T6 runs, unactioned.**~~ **Closed 2026-08-12 in v1.16.0** — all five actioned: instrument-console's table declares its base (palette gate 596 → 603 checks), `--accent-ink` restated at 6.17, the kit's `DataTable` gains the selected state the pack mandates, `workbench` ships its type and spacing tokens and its kit drops to zero raw font-sizes, and `scenarios.md` states that a run against this checkout is not blind. Original row: `workbench` ships no type-size or spacing tokens, so the craft bar's "no ad-hoc font size" cannot be met in it and its kit writes 20 raw `font-size:` declarations — either the clause does not apply to this pack or the token layer is incomplete, and nothing says which. The kit's `DataTable` has no selected state though the pack mandates `--accent-weak` plus a 2px inset. `instrument-console`'s palette table declares no measurement base, so none of its per-token ratios are gate-covered, and it states `--accent-ink` at 6.0:1 where the tokens compute 6.17. And `test/scenarios.md` carries the expected answers beside the prompts, so no run against it can be blind. | T5/T6 runs, 2026-08-12 | medium | **resolved** |
| B-013 | **59% of the library's contrast claims reach no check, and the guard is real work rather than a regex.** Measured 2026-08-12: 121 stated ratios, 71 unguarded — six packs declare no table base, and packs that do still leak because a Gotchas paragraph is not a table row. All 71 were recomputed by hand that day and all 71 are correct, so this is a guard against future edits, not a live wrong number. Two attempts were written and discarded: pooling every token pair **cannot fail** (a planted 9.99:1 passed, ~435 pairs span the whole range), and pooling the token named on the line fails on nine lines of which eight are correct writing — floors, bounds, gradient positions, and candidate colours a pack measures in order to reject. The check therefore has to distinguish a measurement from an argument about a measurement. The honest interim state is written into `validate_palette.py` at the skip. | T10 run, 2026-08-12 | high | open |
| B-014 | **Re-filed 2026-08-20: the reachability half is measured clean and only the mechanical guard remains.** Checked with `for t in $(git tag -l 'v*'); do git merge-base --is-ancestor "$t" origin/main || echo "$t"; done` — **no output**, so all 63 tags are ancestors of `origin/main` and a fresh clone reaches every published release. What is still open is the guard, and it is now cheap to state: the release workflow should refuse a tag whose commit is not an ancestor of `origin/HEAD`. `validate_release_register()` added the neighbouring half this pass — a CHANGELOG release with no tag at all is now reported against a declared list — but it deliberately does not reach for the remote, because `npm test` must work offline. Original row: **A published tag sits ahead of `origin/main`, and a clone cannot reach it.** Original row: **Half-closed 2026-08-12 — `origin/main` now contains the v1.18.0 commit,** because this run pushed the three commits it inherited along with its own; a clone reaches every published tag again. The mechanical half is still open. Original row: **A published tag sits ahead of `origin/main`, and a clone cannot reach it.** Measured 2026-08-12 during the `datasheet` run: `v1.18.0` is tagged (`e3bde7a` → `0c23558`), on `origin`, and served by npm, while `main` was **two commits ahead of `origin/main`** — so the published release lives at a commit a fresh clone does not have, and `origin/main` still reads the `v1.17.0` era. The tag was pushed and the branch was not; the release workflow fires on the tag, so nothing failed loudly. Fix is two commands and the second is the one that gets forgotten. Decide whether the release workflow should refuse a tag whose commit is not an ancestor of `origin/HEAD` — that is a mechanical guard for a class this repository has now hit twice (see the `v1.4.0` ghost, 2026-08-04). | `datasheet` run, stage 0 | high | open |
| B-015 | ~~**A scratch screenshot is committed at the repository root.**~~ **Closed 2026-08-12 in v1.21.0** — `.tmp-fp-hero.png` removed from the tree and `.gitignore` given the `.tmp*` rule that would have stopped it. The run that closed it generated six screenshots and three JSON measurement dumps of its own, all of them outside the repository in a scratch directory, and committed an explicit path list. Original row: **A scratch screenshot is committed at the repository root.** `.tmp-fp-hero.png`, 646KB, entered history in `0c23558` by a `git add -A` that also swept another run's in-flight brief. Nothing references it; `package.json`'s `files[]` excludes it, so it does not reach npm, but it is in the GitHub tree and in every clone. Remove it, and consider whether `.gitignore` should carry a `.tmp*` rule — the retrospective's 2026-08-04 entry already prescribes committing an explicit path list rather than `-A`, and this is what the prescription costs when it is skipped. | `datasheet` run, stage 0 | low | **resolved** |
| B-016 | **Re-filed 2026-08-20: the accent map is derived and correct; the STATUS map was the live instance, and it is now a checked table.** The accent half needs no count — `SURFACE_COMPOSITION.md` says `--accent` in twenty-seven with `--brand` in `field-notes` and `--cta` in `orchard`, each declaring `@role accent:`, and `validate_core_vocabulary()` resolves the role in every pack. The status half was still prose — *"the pair `--ok` / `--warn` in `workbench` and `instrument-console`; the pair `--good` / `--warning` in blueprint, cyclorama…"* — and at twenty-nine packs it was wrong in three places: `workbench` had grown a full quartet, `atrium` had `--info` and `--danger` the map never mentioned, and `datasheet` was listed as having no status palette while it ships four. It is a table now, and `validate_status_vocabulary()` compares every row against the token layers with set equality, requires every pack to appear, and refuses a row naming a pack that does not exist. Its plant shrinks `roster`'s set by one token. What is left of this row is the general class the accent count belonged to: a count inside a sentence, where the noun after the number is not a counted noun, still reaches `validate_counted_claims()` only if the phrasing happens to match. Original row:  Original row: **A second instance was found and fixed by hand at sixteen packs; the class is still open.** `SURFACE_COMPOSITION.md` said the accent role resolves to `--accent` in **thirteen** — recounted from the token layers on 2026-08-12 as fourteen of sixteen, and corrected. It reaches no check for the same reason as before: `in thirteen,` is not followed by a counted noun. Original row: **An unguarded count was found and fixed by hand, and its class is still open.** `SURFACE_COMPOSITION.md` said the accent role resolves to `--accent` in **ten** packs: true at twelve packs, silently wrong at thirteen, now twelve at fourteen. It reaches no check because `in ten,` is not followed by a counted noun, so `validate_counted_claims()` cannot see it. The manifest half of this class was closed in 1.19.0 by widening the source list; this half needs the check to recognise a count whose noun is implied by context, which is the same 'tell a measurement from an argument about one' problem as B-013 and should probably be solved with it. | `datasheet` run, stage 5 | medium | open |
| B-017 | **`field-notes` renders two provenance states in nearly one colour, and the palette gate structurally cannot see it.** Reproduced 2026-08-12 with the gate's own OKLab metric: `--brand-ink` `#8f3f1f` (which `tokens/field-notes.css:54` assigns to `[INFERRED]`) and `--witness-ink` `#9a3016` (`[AMBIGUOUS]`) are **3.21** apart against that file's own disclosed hard floor of 10 — in the pack whose reason to exist is provenance, on the two labels a reader most needs to tell apart. Separately `--witness` `#b3402a` and `--danger` `#c0442e` are **2.84** apart while `field-notes.md:110` says danger is "kept distinct from `--witness`" and its Bans forbid conflating them. **Neither pair is ever compared**: `validate_palette.py`'s peer set is `STATUS_TOKENS` plus `--accent`/`--primary`/`--cta`, so `--brand-ink`, `--witness-ink`, `--verify-ink` and `--witness` are all excluded by name. Two halves: widen the peer set to cover a pack's declared semantic roles (the `@role` marker already exists as a precedent), and decide the colour fix — which changes a shipped pack's hexes and is not a quiet edit. | T24b run | high | open |
| B-018 | **`field-notes` ships a complete dark theme that no prose documents.** `tokens/field-notes.css:242-289` declares a `[data-theme="dark"]` twin with ~30 overrides; the pack's Palette documents only the light values and mentions the dark once, obliquely, in Gotchas. Three consequences reported by the T24b run and each traceable to a line: `--hero-dawn` re-declares no `--dawn-*` stop in dark, so the gradient runs near-white at 98% into the dark `--bg` at 100% — the hard edge the pack's own Bans forbid; the dark `--bg` `#14110e` is the warm-brown palette the pack's Gotchas say it discarded in favour of the forest family; and `--surface-2`/`--surface-3` collapse to one value, removing the step the Palette table calls the deepest. Also: `SKILL.md`'s Figma section lists the packs with a mode collection and omits this one, so publishing its variables would miss a whole mode. | T24b run | medium | open |
| B-019 | **Three routing ambiguities in `SKILL.md`, found independently by both T24 branches.** (1) `(standalone)` marks `workbench`, `briefing-room` and `field-notes` identically, but the motion section then exempts `field-notes` from the hard ceiling the other two carry — a reader who chooses off the table and stops takes a ceiling that does not exist. (2) The dial table's precedence rule is "the row that names the surface wins over the row that names a mood", and both branches hit a brief where the competing row ("trust-first: regulated, public-sector, clinical") names neither — it names an audience posture — so the tiebreak does not arbitrate a two-to-one spread on MOTION. (3) "Mount the candidate packs on an existing, populated page" and "if there is genuinely no populated page yet, the choice is premature" cancel each other for a greenfield marketing site, which is the commonest case for a new pack. Also minor: the word `widened` never appears in any pack-selection text although every non-core pack declares it, and "Fetch the kit first" is written unconditionally inside a paragraph scoped to core packs. | T24a + T24b runs | medium | open |
| B-020 | **The pack skeleton still teaches a radius rule that a shipped pack records as a defect.** `STYLE_PACK_TEMPLATE.md` states concentric radii as subtraction — "an inner radius is the outer radius minus the padding between them" — and `field-notes.md` records that formulation as a shipped error it corrected to a proportional rule. The template is the file `SKILL.md` tells an author to copy, so the corrected error is still the library's authoring default. `datasheet` was authored against the subtraction form and its reference happens to satisfy it exactly (16 − 8 = 8), which is why this did not surface as a contradiction in the new pack. Decide which rule is right and make the template and both packs agree. | T24b run | medium | open |
| B-021 | ~~**`showroom`'s status chip fails contrast.**~~ **Closed 2026-08-13 in v1.26.0** — the label is `--ink` at 14.65–14.73:1 and the status colour is the chip's tint plus a 6px dot, corrected in the pack and the kit with the measured numbers (2.03 / 1.54 / 2.65 against the fills the kit actually used). The three tokens are declared `@role non-text:`, and `validate_status_on_field()` now checks the class. Original row: **`showroom`'s status chip fails contrast, and the chip is the pack's own remedy for colour-only status.** Reproduced 2026-08-12 with this repository's palette gate: at the 12px the pack specifies, `--good` `#0fc27b` is **2.33:1** on white, `--warning` `#f5b900` **1.78:1**, `--danger` `#ff5b59` **3.05:1** — all far under AA, and lower again on the ~12% tint the pack puts them on. `showroom.md:115-119` introduces the chip precisely because those hues separate by only 4.9 under deuteranopia, so the remedy's own text is less readable than the problem. There is also no `--good-ink` / `--warning-ink` / `--danger-ink` and no 12%-tint token in the layer, so "that status's ink" and "~12%" must be invented at the keyboard against the craft bar's no-literals rule. Fixing it changes a shipped pack's hexes, which is not a quiet edit. | T25b run, 2026-08-12 | high | closed |
| B-022 | ~~**`showroom` specifies its specimen frame's radius twice.**~~ **Closed 2026-08-20 in the self-contradicting-pack pass.** The kit is the tiebreak — `kits/showroom/src/styles.css:236` sets `.sw-specimen { border-radius: var(--radius-3xl) }` — so the Texture bullet was the wrong half and now reads `--radius-3xl`, with the 4px inheritance spelled out. The class has a sweep: `validate_radius_single_valued()` attributes a radius token to the noun that FOLLOWS it, so the nesting sentence every pack writes reads as one rule per noun rather than three contradictions, and a component with two radii fails. Its plant flips the Components row back to `--radius-2xl` and is watched being refused. The sweep found a second instance while it was being written: `scoreboard`'s radius-arithmetic example called its subject a *card* at `--radius-md` while the Components table and the kit both set the card to `--radius-sm`; the example now names the report surface, which really is `--radius-md`. Original row: **`showroom` specifies its specimen frame's radius twice, differently — and the frame is the pack's signature.** `showroom.md:161-163` (Texture) nests "a `--radius-sm` chip inside a `--radius-lg` row inside a **`--radius-2xl` specimen frame**"; `showroom.md:177` (Components) gives the frame **`--radius-3xl`**. Reproduced: `--radius-2xl` is 16px and `--radius-3xl` is 20px. The same sentence carries a subtraction rule keyed to the outer value, so every inner radius inherits the error. The same chain also requires a "`--radius-lg` row" where the data row is specified with no radius at all, which strands the Loader's "same radius". | T25b run, 2026-08-12 | medium | **resolved** |
| B-023 | ~~**Two `showroom` tokens ship one hex for two jobs.**~~ **Closed 2026-08-20.** Both halves: `--accent-soft`'s comment no longer claims to be the focus ring's colour (`--ring-focus` has derived from `--accent` since 1.30.0, and the comment named the old value for fourteen releases), and the `--line-weak`/`--surface-2` collision is now a stated decision — `@role drawn-on: --bg, --surface` in the token layer plus a Texture bullet saying the well takes no hairline and that `--line` is 1.24:1 against it if one is genuinely needed. The class has a sweep in the palette gate, `validate_line_surface_collision()`: a line token and a surface token resolving to the SAME colour fails unless the line declares which surfaces it is drawn on. Identity rather than a low-contrast threshold, because this library's hairlines are deliberately near-invisible — `showroom`'s is 1.15:1 on `--bg` and correct. It found the unrecorded second instance the moment it ran: `atrium`'s `--line-ink` and `--surface-ink` are both `#2A2B2F`, and `--line-ink` is the ghost-control border while `--surface-ink` is the banner a ghost control most plausibly sits on — declared now, with `--on-ink` (13.47:1) named as the border there. And a third: `maquette`'s `--model-line` IS `--bg`, on purpose, which the prose argued and the layer did not. Two plants: the undeclared collision, and a `drawn-on` list naming a token the pack does not ship, which is the escape hatch's own bypass. Original row: **Two `showroom` tokens ship one hex for two jobs, and one comment documents a colour the code does not use.** Reproduced: `--surface-2` and `--line-weak` are both `#edeff3` (contrast 1.00:1), so a `--line-weak` hairline on a `--surface-2` well — including the Loader's own row rules, since the skeleton fills with `--surface-2` — is invisible, while the pack's Bans assume hairlines divide. Separately `--accent-soft` `#94b9ff` is commented "the focus ring's colour" and `--ring-focus` is `0 0 0 3px rgba(38, 109, 240, 0.35)`, which is `--accent`; `--accent-soft` appears in no Palette table and the pack bans a second accent. | T25b run, 2026-08-12 | medium | **resolved** |
| B-024 | ~~**Three `showroom` motion values contradict the doctrine the same bundle ships.**~~ **Closed 2026-08-20, and the doctrine took two of the three corrections.** The nav's `0.3s` was written as a literal in two places while `--dur-base` held that exact value: both now name the token, and the doctrine says what it meant — *at or under 300 ms* — so a control may sit ON the ceiling with a reason and 301 is over. `--dur-panel` at 500 ms is an **entrance**, and the doctrine now states that an entrance is not UI motion and may run longer when measured; the pack says which rule its token answers to. The `sub-500ms` sentence in `SKILL.md`, `SHELEG_DESIGN.md`, `README.md` and the Cursor rule was the wrong half — it lumped hover with entrance and set a bound that `manpage` (600 ms), `tenor` (640/820 ms), `bulletin`, `scoreboard`, `field-notes` and `roster` (500 ms) all exceed with measured values. `validate_motion_bands()` parses the press band and the ceiling OUT of `MOTION_DOCTRINE.md` rather than repeating them, and it found the new instance named in this row's re-file: `prism` prescribed its CTA press over `--dur-fast` at 200 ms with nothing faster in the layer, so the pack gained `--dur-press: 0.14s`. Three plants. Original row: **Three `showroom` motion values contradict the doctrine the same bundle ships.** Reproduced against the files: the nav transitions over **`0.3s`** (`showroom.md:283-285`) where `MOTION_DOCTRINE.md` states "**UI motion stays under 300 ms**" — 0.3s is not under 300ms, and the pack's stated rationale is that this is "slower than everything else on the page", which is also false against its own `--dur-panel` of 0.5s. `--dur-panel` **0.5s** is then spent on every section reveal (`showroom.md:265-266`) against `SKILL.md`'s "hover/entrance motion stays **sub-500ms**" — legal for a drawer, out of range for an entrance. And the panel curve `cubic-bezier(0.65, 0, 0.35, 1)` is an ease-in-out on an *entering* element, which the doctrine routes to ease-out and for which `--ease-drawer` already exists. | T25b run, 2026-08-12 | medium | **resolved** |
| B-025 | **The pack skeleton's Components contract lists classes no pack answers, and a widened pack has no backstop.** `STYLE_PACK_TEMPLATE.md` requires each Components entry to state "resting state, hover, active and disabled" over a list including **Inputs / forms**, **Loaders** and **Empty states**, and requires Navigation's "resting shape, scrolled shape, **mobile shape**". `pigeonhole` specifies none of the three classes (not even as N/A) and several of its entries are rest-only; `SKILL.md`'s materialized-kit paragraph is scoped to what a **core** pack leaves out, so a widened pack's silences have no filler. Decide whether the skeleton's list is the contract (in which case ten widened packs owe entries) or whether it is a menu — and say which, where an author reads it. Found by T25a against `pigeonhole`; the shape is library-wide. | T25a run, 2026-08-12 | medium | open |
| B-026 | **`pigeonhole` depends on an exclusion that only prose enforces.** Its nine `--cat-*-ink` tokens are deliberately outside `validate_palette.py`'s semantic peer set, because eleven hues cannot be simultaneously AA-compliant and mutually distinguishable — measured: the worst deuteranopic pair is 4.42 ΔE as the reference paints it and 1.24 after derivation, against a hard floor of 10. The exclusion holds because `STATUS_TOKENS` matches by NAME, which is exactly the defect shape B-017 describes: a pack's declared semantic roles are invisible to the peer check. So the decision cannot be verified and cannot be violated loudly — an author who renames a category token `--danger-ink` gets a red gate for the right reason by accident, and one who adds a tenth hue gets nothing. Fix with B-017's widening: when the peer set learns to read a pack's declared roles, it also needs a way for a pack to declare a set as **redundantly encoded** and have that declaration checked (the `never by colour alone` phrase already exists as the soft-floor escape; this is the hard-floor case). | `pigeonhole` run, stage 10 ladder walk | medium | open |
| B-027 | **42 declarations across eight token layers restate a token's channels by hand, and the gate can now read the derived form.** Measured 2026-08-13 by sweeping every `rgba()` literal against the tokens in its own file: 42 are ΔE 0.00 from a token, so `rgb(from var(--token) r g b / a)` is a byte-for-byte replacement — `instrument-console` 3, `showroom` 8 (one migrated in 1.22.0 as the worked example), `orchard` 4, `scoreboard` 4, `maquette` 2, `field-notes` 1, and the rest spread thin. Migrating closes B-023/B-024's mechanism: a hand-derived literal stops tracking its source the moment either moves. **The decision this needs is per property, not per pack**: relative colour is Baseline 2024, so a dropped declaration is acceptable for a decorative tint and unacceptable for a focus ring, where the literal must ship first as a fallback. Do it pack by pack, propagating to the `.cursor` mirror and the kit's token block each time (the gate catches both). | `2026-08-13-modern-css-audit` | medium | open |
| B-028 | **13 `rgba()` literals sit within ΔE 2 of a token without equalling it, and only their author knows which are derivations.** Measured 2026-08-13: `datasheet` 5, `field-notes` 2, `orchard` 2, `editorial-luxury` 1, `pigeonhole` 1, `workbench` 1, `datasheet`'s `rgba(232,232,232,0.1)` the furthest at ΔE 1.78. **Near is not drift**: `rgba(255,255,255,0.8)` beside an off-white `--bg` may be deliberately pure white. Each case is a question — was this meant to be that token? — and answering it wrong changes a shipped colour. Not a sweep; a review, one pack at a time, and the answer belongs in the token layer's comment either way. | `2026-08-13-modern-css-audit` | low | open |
| B-029 | **Re-filed 2026-08-20: it is 15 of 29 kits, not 0 of 16.** Recounted with `grep -rlE '@container|cqi' kits/*/src/*.css kits/*/src/*.tsx | cut -d/ -f2 | sort -u | wc -l` → **14** kits answer their own width (`almanac`, `blueprint`, `bulletin`, `daylight`, `ledger`, `notation`, `ora`, `paperclip`, `roster`, `router`, `scoreboard`, `tenor`, `vitrine`, and `field-notes` via its declared blocks), so **15 remain** and `validate_kit_breakpoints()` has been gating the class since 1.23.0 — a viewport query in a kit now needs `PAGE` / `SELF` / `TODO-CONTAINER` with a reason. What is left is the migration, not the guard. Original row: **The component library breaks at viewport breakpoints although every component knows its own width.** 0 of 16 kits use `@container` or `cqi`/`cqw`; 7 viewport `@media` blocks do the work. Original row: **The component library breaks at viewport breakpoints although every component knows its own width.** 0 of 16 kits use `@container` or `cqi`/`cqw`; 7 viewport `@media` blocks do the work. `field-notes.md:271-274` reasons about container queries and lands on "not for the page, whose column widths are the layout" — sound for a page pack, and it never reached the kits, which are components a consumer drops into a sidebar. Scope: `container-type: inline-size` on the kit's card/row/panel roots plus `@container` breakpoints, with the viewport queries kept for page-level composition. The pack contract's `## Responsive` section would need a sentence about which of the two a value belongs to. | `2026-08-13-modern-css-audit` | medium | open |
| B-030 | **Five typographic primitives the library specifies by hand and the browser now does properly.** (1) `text-box-trim` / `text-box-edge`: 0 uses, in a library that states line-height to three decimals — this is what makes a 60px/1.0 display measure its cap height. (2) Measure in `px`: 3 layers declare a px measure, **0 declare one in `ch`**, so a measure stops being a measure when the typeface changes. (3) Metric-matched fallbacks: `font-display` in 21 files, `size-adjust`/`ascent-override` in none — `swap` without them is a guaranteed reflow. (4) `@property`: 0 of 16, so no token is typed or animatable. (5) Fluid spacing: 0 of 16, while type is fluid in 5 — a page that scales its headline and not its rhythm changes proportion as it narrows. Each is independent; take them one at a time, each with a browser-support line in the pack. | `2026-08-13-modern-css-audit` | medium | open |
| B-031 | **The token layers cannot be read by any tool but a browser.** No `$type`, no `$value`, no DTCG JSON — so Tokens Studio, Style Dictionary and Figma's variable import all need a hand transcription, which is the failure mode `FIGMA_BRIDGE.md` exists to prevent. A generator from `styles/tokens/*.css` to DTCG would make the Figma seam mechanical, and it is the one item on this audit that adds a build step rather than a CSS feature — decide whether that belongs in this repository or in the workbench. | `2026-08-13-modern-css-audit` | low | open |
| B-032 | **Two kit breakpoints have a real container answer and are still on the viewport, each with its reason written at the block.** `blueprint`'s registration tick and column rule are descendants of the panel and the column; `datasheet`'s instrument cell grid is a descendant of the instrument, and that pack's Responsive section now says it must collapse when its own box is narrow rather than when the phone is. Both are marked `TODO-CONTAINER B-032` and both are held out of 1.23.0 for the same reason: the breakpoint has to be **derived from the component's own geometry**, the way the scoreboard ledger's 231px was (label 95 + gap 12 + numeral 80 + gap 12 = 199, plus a 32px minimum leader), and carrying 768 or 640 across into `@container` would be the same mistake in a newer syntax. Each needs its own arithmetic and a look at what the component's minimum comfortable column actually is. | `2026-08-13-modern-css-audit` → 1.23.0 | medium | open |
| B-033 | ~~**`scoreboard`'s token layer still licenses what the pack retracted in 1.13.1.**~~ **Closed 2026-08-20.** The comment on `--accent-hover` carries the retraction instead of the permission: it is the hover step of a mark, it may not carry a link either, and a link is `--ink` with an `--accent` underline. The 4.12:1 measurement stays, because it was never the wrong part. Original row: **`scoreboard`'s token layer still licenses what the pack retracted in 1.13.1, and a shipped comment outranks a correction nobody reads.** `tokens/scoreboard.css:50` carries `--accent-hover: #e03d00; /* 4.12:1 on --bg — the one orange that may carry a link */` while `scoreboard.md:371-375` retracts exactly that sentence: *"[CORRECTION — 1.13.1] No orange in this pack can carry a link."* Reproduced 2026-08-13: both strings are present in the shipped files. `SKILL.md` tells an implementer to copy the token layer verbatim, so the retracted permission is the one that reaches them. Fix the comment, then sweep every other pack for a correction that landed in prose only. | T26b run, 2026-08-13 | high | **resolved** |
| B-034 | ~~**`scoreboard`'s dark band inherits the paper status set.**~~ **Closed 2026-08-13 in v1.26.0** — the `[data-surface="panel"]` block now remaps all four statuses to the on-dark values the pack already prescribed (10.21 / 11.49 / 6.92 / 7.51 against that band, where they had been 3.69 / 3.94 / 2.37 / 2.90). The `--ink-faint` half is closed too: it is not remapped and does not need to be, because the panel's `--ink-soft` is a different value now that the statuses no longer borrow it. Original row: **`scoreboard`'s dark band inherits the paper status set, which its own Bans forbid.** Reproduced 2026-08-13: the `[data-surface="panel"]` block remaps eleven tokens — `--bg`, `--surface`, `--surface-2`, `--surface-3`, `--ink`, `--ink-strong`, `--ink-soft`, `--line-weak`, `--line`, `--action`, `--action-hover`, `--on-action` — and **not** `--good`, `--warn`, `--danger` or `--info`. So `var(--good)` inside the ledger paints **3.69:1** on the dark band where `--good-on-dark` would give **10.21:1**, and `scoreboard.md:348-349` bans precisely that: *"The paper status set on the dark band, or the dark set on paper. They are two sets."* The block already proves it knows the mechanism, because it remaps `--ink-soft` for the same reason. Same block: `--ink-faint` is not remapped and collapses onto the panel's `--ink-soft` (both `#9f9fa9`), so a disabled control is indistinguishable from secondary copy on the pack's hero surface. | T26b run, 2026-08-13 | high | closed |
| B-035 | ~~**Eight `scoreboard` numbers or descriptions disagree with the values beside them.**~~ **Elevation half closed 2026-08-20; the rest is re-filed as B-047.** The elevation comment counted "two soft stacks and a hairline" over four shadows, called `--shadow-card` untinted when `rgba(16, 24, 40, …)` is a blue-black, and left `--shadow-panel` in no prose at all. Both the comment and the Texture bullet now name all four with their layer counts, alphas and jobs, and say the tint is deliberate. `validate_elevation_tokens_named()` is the sweep: every `--shadow-*` / `--*-glow` a pack declares must be named in the pack's prose. It returned **sixteen** unnamed tokens across eleven packs on its first run — `almanac`, `awning`, `briefing-room`, `editorial-luxury`, `maquette`, `notation`, `orchard`, `pigeonhole`, `proscenium`, `router`, `workbench` — all now named, and `proscenium`'s own "Elevation is three shadows" turned out to be four. Original row: **Eight `scoreboard` numbers or descriptions disagree with the values beside them.** Reproduced where numeric: `#00D492` is stated at 1.84:1 *"on white"* and measures **1.94** there — 1.84 is its ratio against the paper, and the sentence names the white metric cards. `--shadow-card` is described as *"untinted"* and ships `rgba(16, 24, 40, …)`, a blue-black; the same comment says *"Both stacks are two layers at 4–5%"* where `--shadow-lift` is 16%. *"Elevation: two soft stacks and a hairline"* counts three where four ship, and `--shadow-panel` appears in no prose. The sand focus ring's only stated ratio is against `--bg` for a token whose role is `--surface-sand` only (15.88 stated, 14.60 on its actual ground). The on-dark status range is given as 1.6–2.6:1 against paper where the five values span 1.33–2.72. None is an accessibility failure; all are the class the pack's own 1.13.1 correction exists to police. | T26b run, 2026-08-13 | medium | **resolved** (rest → B-047) |
| B-036 | ~~**`scoreboard` bans radii above 8px and its signature motif is a pill.**~~ **Two of four halves closed 2026-08-20; the token-less literals are re-filed as B-048.** The radius ban now reads *"Radii above 8px — except `--radius-pill`"*, which the section tick needs and the Components table requires, and says the pill is for the tick and nothing else. The pixel-numeral ban no longer cites `font-smooth: never`, which is not a legal value of the property the pack sets and is not a property any engine implements: it names the declaration the Type section actually ships and states plainly where it has no reach — Firefox on macOS has only `-moz-osx-font-smoothing: grayscale`, and Windows has nothing. Original row: **`scoreboard` bans radii above 8px and its signature motif is a pill; three of its stated behaviours have no token, and one has no working mechanism.** The Bans forbid *"Radii above 8px"* while `--radius-pill: 999px` ships and the section tick requires it — the Texture enumeration omits the pill entirely. The display's 42px and 44px steps and the ledger's 75px/95px label columns exist as prose literals with no token and no media query, against the craft bar's *"tokens, not literals"*. The secondary button is specified as *"`--surface-2` fill at 4% ink, `--ink` at 50%"*, where 4% ink over the paper resolves lighter than the declared `--surface-2` and *"`--ink` at 50%"* measures 3.24:1 on it — below AA for the 10–12px label the same row specifies. And the pixel-numeral requirement cites two different properties (`-webkit-font-smoothing: none` in Type, `font-smooth: never` in the Bans) of which the second is not a legal value of the first and neither disables antialiasing outside WebKit — so a non-negotiable typographic rule has no mechanism on Windows or Firefox. | T26b run, 2026-08-13 | medium | **resolved** (rest → B-048) |
| B-037 | ~~**The retirement trigger for standing instructions is computed from annotations runs forget to update.**~~ **Closed 2026-08-20, with SG-04.** All three registers in `docs/evidence/retro.md` stopped at `v1.26.0` / 2026-08-13 while `1.44.0` shipped — eighteen versions — so the trigger at line 11 (*has not fired in five run stamps*) read every standing instruction as dormant and any of them as retirable on a count that did not exist. The Run stamps table is now complete to `v1.44.0`: twenty-six rows derived from `git log`, the tags and the CHANGELOG, with `Diverged?` left *unrecorded* rather than guessed, exactly as the 2026-08-12 backfill of the `v1.14.1`–`v1.18.0` gap did. `validate_release_register()` fails when a CHANGELOG release at or after `1.5.0` has no row, so the register cannot fall behind silently again; it reads table ROWS only, because the section's own header sentence names the range it covers and the first draft let that sentence stand in for twenty-six rows — the plant deletes one row and was watched being refused. Original row: **The retirement trigger for standing instructions is computed from annotations runs forget to update, so an instruction that fires every release can look dormant.** Reproduced 2026-08-13 while pruning: instruction 2's note reads *last fired 2026-08-12 `e9b753f`*, thirteen run stamps back, and by the letter of *has not fired in five run stamps* it should have retired — while stamps 21, 22 and 23 each read the registry before their tag, which is the instruction firing. The same gap sits on 3, 4, 5 and 8: stamp 23 alone evidences firings none of their notes record. The list is capped at ten and the cap is enforced by an arithmetic whose inputs are the part of the close-out a run does last, so the failure mode is retiring a live instruction rather than keeping a dead one. Two candidate fixes: give each run stamp a column naming the instructions that fired (the trigger then computes from the table rather than from prose), or make the prune's own gate criterion 'every instruction that fired has its note updated in this commit'. The first is mechanical and the second is a habit. | the `a9c497e` prune walk, 2026-08-13 | medium | **resolved** |
| B-038 | ~~**`instrument-console` legislates for four statuses and ships two colours.**~~ **Closed 2026-08-20 by shipping the two it was missing.** The prose has required success, warning, danger and info together since the pack's first release, so `var(--danger)` and `var(--info)` were undefined custom properties — invalid at computed-value time, falling back with no error anywhere — in the pack that legislated them. `--info: var(--accent)` is the signal hue by decision (an infrastructure console's *here is a fact* and its *here is the thing to press* are one blue, and a separate hue would break the pack's one-accent rule; the palette gate reports it as an alias rather than a collision). `--danger: #f4526b` is a **pack decision, not a measurement** — the reference paints no danger state, so there was nothing to measure — chosen to clear AA on every field the pack ships (6.02:1 on `--base`, 5.77 on `--surface-1`, 5.10 on `--surface-3`) and to clear every separation floor against every peer including under simulated protanopia, deuteranopia and tritanopia (worst pair 10.3 against a floor of 8.0). The Bans line now names the set it permits. The pack's row in `SURFACE_COMPOSITION.md`'s status map moved with it, and that map is a checked table now rather than a sentence. Original row: **`instrument-console` legislates for four statuses and ships two colours, and its own waiver says the pair fails the gate.** Reproduced 2026-08-14: `tokens/instrument-console.css` declares `--ok: #46d39a` and `--warn: #e0a030` and **zero** `--danger` / `--error` / `--crit` / `--info` tokens, while the pack's Bans read *"**Success, warning, danger and info** always ship with an icon or a word beside the fill"* and the next line forbids adding one — *"no second color except `--ok`/`--warn` status semantics"*. Its Bans also record *"several of these pairs sit inside a dichromat's confusion line; re-stepping them would invent a colour this pack does not own"*, i.e. a prose waiver where `STYLE_PACK_TEMPLATE.md` rule 3 prescribes deriving the value and marking it derived at the declaration (`maquette` is the worked example). A page whose sections are readings **against a threshold** — the pack's own register — has no legal colour for the breach state. | T29b run, 2026-08-14 | high | **resolved** |
| B-039 | ~~**`instrument-console`'s pressed primary drops its label to 3.06:1.**~~ **Closed 2026-08-20.** The press dims the fill to `--accent-dim` **and swaps the label to `--ink`**, which is 5.62:1 there; `--accent-ink` is 6.17:1 on `--accent` and 3.06:1 on `--accent-dim`, and the Palette row says so on the same line as the warning it sat under. Each ratio is written with its partner on one line, which is what `validate_stated_ratios()` needs in order to check it — the first draft of this fix wrapped a claim across a line break and the gate refused it, correctly. Original row: **`instrument-console`'s pressed primary drops its label to 3.06:1, and the table reasons about every neighbouring value except that one.** Reproduced 2026-08-14 by recomputation from its token layer: `--accent-ink` `#0a0e14` on `--accent-dim` `#1f5fb0` = **3.06:1**, below AA for the label the pack's Micro-interactions prescribe (*"press = 1 shade dimmer (`--accent-dim`), no bounce"*). The pack states the resting pair correctly (6.17:1, verified) and even warns that `--accent-dim` is *"a fill, not a label"* — one row above the rule that makes it one. Fix by pressing with opacity or a border rather than a fill swap, or by giving the pressed state its own label token. | T29b run, 2026-08-14 | high | **resolved** |
| B-040 | ~~**`instrument-console` is one of only two token layers with no `prefers-reduced-motion` branch — and it is the pack that mandates a particle field.**~~ **Closed 2026-08-19** — the promise now has an observable. `validate.py`'s `validate_reduced_motion()` reads every token layer and asserts two things per layer: a layer declaring `--dur-*` or `--ease-*` ships a `@media (prefers-reduced-motion: reduce)` branch, and that branch collapses at least one duration to an instant value (≤1ms). The second half is the point of the row — the only thing that had been reading "degrade to calm" was `sloplint.py:208`, which asserts the STRING `prefers-reduced-motion` occurs in `MOTION_DOCTRINE.md`, so a branch that mentioned the query and collapsed nothing would have satisfied every gate. Durations are matched by VALUE, not by name: `paperclip` spells its durations `--t-micro … --t-hero-copy` and a name-keyed check would have read its branch as empty. Re-counted at the close: **29** token layers, **29** declaring motion, **2** with no branch, **0** now. Both fixed — `editorial-luxury.css` collapses all eight of its durations; `instrument-console.css` collapses its four plus `--stagger`, and because a duration cannot stop a rAF loop, `instrument-console.md`'s `## Motion flavor` now states the still for each of its four motifs (particle field → one static frame, scan/dim spotlight → every section lit, scrubbed instruments → fully drawn, rail → already static). The word *reduced* did not occur in that pack at all, which was the other half of this row. Two plants in `PLANTS` and one CI negative control, each asserting the message and not merely the red: the self-test grew an optional `expect` field because every token layer is also copied into its kit byte for byte, so any edit trips the kit-drift check and a plant would have reported *caught* with this check never having run. Floors 3333/1912/629 → 3391/1912/635 (+58 = 29 × 2, +6 = one new sloplint target × six bans, measured). Residual split out as B-045. Original row: **`instrument-console` is one of only two token layers with no `prefers-reduced-motion` branch — and it is the pack that mandates a particle field.** Reproduced 2026-08-14: `grep -L prefers-reduced-motion styles/tokens/*.css` returns exactly `instrument-console.css` and `editorial-luxury.css`; eighteen of twenty ship one. The word *reduced* does not occur anywhere in `instrument-console.md`, which prescribes a WebGL particle field, a scan/dim spotlight and scrubbed instruments. `MOTION_DOCTRINE.md` §9: *"Shipping an animation without a reduced-motion path is a bug, not a polish item."* | T29b run, 2026-08-14 | high | closed |
| B-041 | ~~**`instrument-console` names its glow twice and bans its own shadow.**~~ **Closed 2026-08-20.** The pack has two glows and says so in all four places now: `--accent-glow` is the focus halo, `--signal-glow` is the composite the one active signal element wears, the Texture section names the token instead of retyping its literal, the Palette table has a row for it, and the Bans permit both. Caught as a class by `validate_elevation_tokens_named()`, which reads `--*-glow` as well as `--shadow-*`: a mandated composite that appears in no prose is a value a reader has to reconstruct from a literal. Original row: **`instrument-console` names its glow twice and bans its own shadow.** Its Palette calls `--accent-glow` *"the only permitted glow"* and its Bans read *"no colored shadows besides `--accent-glow`"* — but `--accent-glow` is a **colour**, and the shadow the pack actually mandates is `--signal-glow`, a two-layer composite in its token layer that the prose never names. Read literally the ban forbids the pack's signature shadow. Compounding it, the Gotchas cap the glow at *"one element per viewport"* while Micro-interactions put it on `:focus-visible` — user-driven, so any keyboard user tabbing while the active signal is on screen puts two in one viewport, and no implementer can keep the budget. | T29b run, 2026-08-14 | medium | **resolved** |
| B-042 | **`instrument-console` contradicts its own `Contract: core` line, and its one site-wide curve collides with the doctrine's scrub rule.** The Contract says the hero, its line ceiling and per-component states are *"yours to decide"*; the pack then states a hero ceiling (*"clamp-scaled (hero ~5.25rem ceiling)"*) and both the button press and the focus-visible treatment. Separately, Motion tokens declare *"the one site-wide curve"* while Signature motifs mandate **scrubbed** SVG instruments, and `MOTION_DOCTRINE.md` §6 requires `ease: 'none'` under scrub — the pack never carves the exception, so an implementer reading it alone lands on the bug the doctrine calls the most common one. Its fastest duration token (0.18s) also sits outside the doctrine's 100–160ms button-press band, so a compliant press needs a literal the craft bar forbids. | T29b run, 2026-08-14 | medium | open |
| B-043 | **`instrument-console`'s token layer is 1.4 KB and the pack spends tokens it does not contain.** Smallest of the twenty (next: `briefing-room.css` at 3.2 KB): no type-scale, weight or spacing tokens beyond `--section`, and no focus-ring token — while `STYLE_PACK_TEMPLATE.md` rule 5 singles out the focus ring as the one place the token form matters most, and the craft bar bans ad-hoc font sizes. Its `## Texture & surface` says *"radii 4 / 8 / 14px (+ pill) — machined, not squircle"* and omits the nesting arithmetic the template says is what produces "machined". And `--section: clamp(9rem, 24vh, 20rem)` drives vertical padding off `vh`, which re-lays out every section when a mobile URL bar shows or hides. | T29b run, 2026-08-14 | medium | open |
| B-044 | ~~**Every pack that bans a weight or a slant owes a base layer, and none of the twenty ships one.**~~ **Closed 2026-08-20 with a sweep.** `validate_emphasis_base_layer()` reads the ban out of the pack's prose and requires the matching base rule in the same token layer. Three packs were missing one and now ship it: `atrium` (`strong, b { font-weight: var(--weight-semibold) }` — the UA's 700 is a weight no face in that pack has, and inside its 300-weight serif the browser fakes it), `notation` (`var(--w-bold)`, which is 500, because the pack has no bold), and `field-notes`, where the answer is a synthesis rule rather than a weight: Bricolage Grotesque has no italic, so `em, i { font-style: italic; font-synthesis-style: none }` refuses the browser's shear without touching Geist's real italic in the body copy. `awning` and `tenor` already shipped theirs. The plant strips `font-weight` from `tenor`'s rule, and writing it exposed a hole in the first regex: `strong, b.disabled { font-weight: … }` satisfied it, leaving every other `<strong>` on the UA default. Original row: Every pack that bans a weight or a slant owes a base layer, and none of the twenty ships one. `<strong>` renders at 700 and `<em>` renders italic with no stylesheet involved, so the violation is invisible to a grep over the CSS and to review. `tenor` 1.33.0 ships the three-line reset; the other nineteen do not, and each needs its own answer because emphasis resolves differently per pack (value, weight, face). Found by walking computed styles on a running page. | `2026-08-15-tenor-product-surface` | high | **resolved** |
| B-045 | **The reduced-motion gate requires one duration to collapse, not every duration, and it cannot tell a declared exception from silence.** `validate_reduced_motion()` (B-040) refuses a layer with no branch and a branch that collapses nothing, so a layer collapsing one of nine durations still passes. Turning the strict form on is not a one-line change, because the exceptions are real and reasoned. Measured 2026-08-19 by value, not by name: **2 of 29** layers leave a declared duration uncollapsed, and both say why — `roster` keeps `--dur-float-a`/`--dur-float-b` at 5.5s/6.5s because they drive infinite animations, a duration cannot stop one, and at 0.01ms it strobes at exactly the reader the query protects (the kit pauses them with `animation-play-state`, which no custom property can express); `atrium` keeps its four `--flute-dur-*` because under reduce the canvas is REMOVED and the still underneath is the hero — *"a slow refraction is still refraction"*. `pigeonhole` is the near-miss: `--dur-marquee` collapses to 0.01ms so it PASSES, while its own comment says 0.01ms is wrong for a marquee and the kit pauses it. So the gate reads the one case that needs a component-layer stop as compliant. The shape to build is `validate_kit_breakpoints`': every declared duration collapses **or** carries a marker naming why, and where the marker's promise is a component-layer stop, that stop is checked in the kit. Both reasons above are already written — as prose the gate cannot read. | the B-040 close, 2026-08-19 | medium | open |
| B-046 | ~~**The counted-claims guard cannot see a number hyphenated onto its noun, and `docs/DOCMAP.md` — the document whose own subject is single homes — carried two stale facts because of it.**~~ **Closed 2026-08-19** (SG-03, manifesto M-44). `COUNTED` required a **space** between the number and its noun, so the CI sentence naming the build matrix sat stale while fifteen more kits landed, with `validate_counted_claims()` running green over the same file three lines above it. The mechanism mattered more than the fact: the B-040 run had already measured the hole and routed around it, deleting a hyphenated `twenty-nine releases` from its own prose rather than plant a number the regex could not police — a guard contributors write around has stopped being a guard. The separator is now `[ -]`, captured and back-referenced so a compound is read in one alphabet or the other; the plural is still required for the spaced form only, because a hyphenated count is a modifier and singular by grammar while `one kit per pack` is a ratio, not a tally. Blast radius measured before shipping, old pattern against new on one tree: **34 → 36** spans inside the source list (`+2`, both in `DOCMAP.md`: `thirteen-heading`, already correct, and the kit count, which was the stale one), **333 → 384** across all 1025 text files (`+51`, of which 3 are dropped by the identifier exemption). **Zero false positives inside the policed set.** One class exempted by pattern — a count glued into a longer identifier (`feat/four-packs-v1.9.0`, 2 occurrences, neither in the source list); one class exempted by the source list staying short — hyphenated compounds counting something other than the library (`a six-pack backfill`, `the nine-heading contract`), which no pattern separates from a stale claim. Second fact: `## Shared state` opened with `ungated` — "no lease mechanism is in force in this repo" — while `.claude/agent-sync.json` carried `"gated": true` over nine paths and lease files sat in `.agent-sync/leases/`. Fixed by **deriving instead of restating**: DOCMAP now points at the generated `docs/AGENT_SYNC.md`, and `validate_coordination_claim()` asserts the page carries its generated marker, that its guarded-file list equals the config's, and that DOCMAP does not re-assert the denial while the page records the repo as gated. Three plants added, each asserting the message via `expect`; `--self-test` 19/19. Floors 3391/1912/635 → 3398/1912/635, the +7 attributed by running each function in isolation against this tree and against `7e630cb` (+2 counted spans, +3 coordination checks, +2 relative links). | SG-03, the manifesto conformance program | high | closed |
| B-047 | **The rest of B-035: five `scoreboard` statements whose numbers or subjects still disagree with the values beside them.** The elevation half closed 2026-08-20; these were reproduced in the same T26b run and are untouched. `#00D492` is stated at 1.84:1 *"on white"* and measures **1.94** there — 1.84 is its ratio against the paper and the sentence names the wrong field; the remaining four are of the same shape. They are individually small and collectively the reason `validate_stated_ratios()` exists, so the work is either to give each claim a partner the gate can read (which is what closes them permanently) or to correct the number in place. Prefer the first: an unguarded correct number is one edit from being an unguarded wrong one, and the count of unguarded claims is now printed on every palette run (`stated ratios: … 177 unguarded (35%) at 22 packs`). | B-035 split, 2026-08-20 | medium | open |
| B-048 | **The rest of B-036: three `scoreboard` behaviours with no token, and one contrast defect.** The display's 42px and 44px steps and the ledger's 75px/95px label columns exist as prose literals with no token and no media query, against the craft bar's *"tokens, not literals"*. And the secondary button is specified as *"`--surface-2` fill at 4% ink, `--ink` at 50%"*, where 4% ink over the paper resolves lighter than the declared `--surface-2` and *"`--ink` at 50%"* measures 3.24:1 on it — below AA for the 10–12px label the same row specifies. The contrast half is the one that reaches a user. Note the shape of the miss: `validate_stated_ratios()` cannot see it, because the claim is an alpha instruction rather than a stated ratio — the pack says *what to composite*, and nothing computes the result. | B-036 split, 2026-08-20 | high | open |
| B-049 | **A duration in a token-layer COMMENT is outside every band check.** `validate_motion_bands()` reads a pack's `## Components`, `## Micro-interactions` and `## Motion flavor` prose, deliberately: those are where a pack prescribes an interaction. It does not read the token layer's own comments, and at least one is a doctrine contradiction — `bulletin`'s `--dur-panel: 0.4s; /* the nav dropdown */` against the doctrine's 150–250 ms dropdown band. Widening the sweep to comments means adjudicating every commented duration in twenty-nine layers, which is a pass of its own and was not this one; the sweep's scope note in `test/validate.py` says so at the function. | the motion-band sweep, 2026-08-20 | medium | open |
| B-050 | **A prescribed foreground/background pair is only checked when the pack states a ratio for it.** B-039 was found by hand: the press dimmed a fill under a label and nothing computed the result, because the pack prescribed *"press = 1 shade dimmer (`--accent-dim`)"* and named no number. The generalisable check is a Components-table sweep — a row names a label token and a fill token in its resting cell, and its hover/active cells name replacement fills, so `contrast(label, new fill)` is derivable for every state of every component in the twenty-two widened packs. `instrument-console` could not be its first catch, because it is a core-contract pack with no Components table at all, which is also why the defect survived there. Not written this pass; the fix for B-039 is one pack's prose. | B-039 generalisation, 2026-08-20 | high | open |
| B-052 | **The description changed and discovery was not re-measured.** B-006 added an `investor deck` trigger, taking the description from 955 to 988 of 1024 chars. Widening a description changes what the runtime matches, and the T1 trigger set — the one that proves a phrasing reaches this skill and not a sibling — has not been re-run in fresh contexts since. The guard added with it proves a surface class is NAMED; it cannot prove a request for that surface arrives here rather than at `super-ux` or `copywriting`. | B-006 close, 2026-08-20 | medium | open |
| B-051 | **The `Diverged?` column of twenty-six reconstructed run stamps says *unrecorded*, so the retirement trigger counts stamps and not evidence.** The Run stamps table is complete to `v1.44.0` again and `validate_release_register()` keeps it that way, but eighteen releases' worth of rows were derived from `git log` rather than written by the runs they describe — and *whether a run hit something worth an entry in the Log* is the one column that cannot be derived, exactly as the 2026-08-12 backfill recorded. Consequence: an instruction that fired in one of those runs looks dormant, and the *has not fired in five run stamps* trigger is arithmetically sound and evidentially empty over that range. The fix is not archaeology — it is that runs from here on write their own row, and that no instruction is retired on a stamp count whose rows are reconstructed. | SG-04 close-out, 2026-08-20 | medium | open |
