# Carry-over ledger — four style packs, v1.9.0

> Append-only. Every stage writes here the moment something is deferred, dropped
> or left half-done. Read in full at stage 10. **Deferred out loud is forgotten.**

| # | Stage | Item | Why deferred | Resolution |
|---|---|---|---|---|
| C1 | 0 | Whether `maquette` is a pack or a variant of `instrument-console` | The two share every surface signal — near-black field, one pale accent, mono labels, an enterprise-infrastructure buyer. Opinion could not settle it. | **Closed at stage 0 by measurement, before any kit existed.** Two subagents in separate fresh contexts against the draft register: A chose `maquette` for an architecture brief and derived the discriminating test unprompted; B stayed on `instrument-console` for a live-telemetry brief and reported `maquette` pulled on surface features only. Fork works both ways → full pack. |
| C2 | 0 | **Five of eight shipped packs name no other pack at all.** Forks exist only in `field-notes`, `cyclorama` and `atrium`, and all point backwards; `instrument-console` — first stop for any infra brief — has none. | Surfaced by the probe subagent, then reproduced by counting fork mentions across all eight packs. Real, and it multiplies with four more one-way forks. | **Stage 4–5** as REQ-012: reciprocal pointers plus a validator check that enforces them. |
| C3 | 0 | `blueprint`'s grid, vertical rules and corner registration marks are not reachable from any stylesheet rule | Tried background-image tallies, SVG `<pattern>` lookup, thin-element scans and absolutely-positioned two-border scans; all empty. They are almost certainly drawn as inline SVG or pseudo-elements the scans missed. | **Stage 4** — describe as observed geometry and mark it, the way `cyclorama` marks its status pill. Never as a measured declaration. |
| C4 | 0 | Licensed display faces: GT Planar (`blueprint`), Tiempos (`showroom`) | Substitutes must be measured, not remembered — `cyclorama` set that bar. | **Stage 4**, before each Type section locks. |
| C5 | 0 | `showroom`'s reference declares **four** type families (Inter, InterDisplay, JetBrains Mono, Tiempos); the pack contract allows ≤3 | Found in the harvest. Needs a decision about which is vestigial, as `cyclorama` did with Urbanist. | **Stage 3**, in the design record. |
| C7 | 6 | **`blueprint` contradicted itself about registration marks** — Signature element said one thing per viewport, Components and Hero gave them to both CTAs. | Surfaced by the T17a subagent, reproduced by reading the three sections. | **Closed in the same run.** Marks belong to the primary only; the Signature element now says so in as many words, because "one thing" is exactly the rule a CTA pair invites you to bend. |
| C8 | 6 | **The three vector-database packs did not fork against each other.** `blueprint` named neither `prism` nor `maquette`; T18a had to derive the distinction itself and said so. | Surfaced independently by two subagents (T18a and T19a), reproduced by grep. The reciprocity work had drawn the new-to-old edges and missed the new-to-new ones. | **Closed in the same run.** All three now fork explicitly, reciprocally, and route on *who the reader is and what the page must contain* rather than on category — which is identical for all three. |
| C6 | 9 | The code graph's doc half is still one release behind | No graphify backend key exists on this machine — checked again at stage 0 of this run: still absent. | **Operator**, carried over from the v1.8.0 run's C11. Unchanged by this run. |
