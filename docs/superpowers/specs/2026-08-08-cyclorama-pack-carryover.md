# Carry-over ledger — `cyclorama` style pack

> Append-only. Every stage writes here the moment something is deferred, dropped
> or left half-done. Read in full at stage 10. **Deferred out loud is forgotten.**

**Close-out count: 11 rows — 10 resolved, 1 open.** The open one is owned by the
operator and is named in the acceptance record as REQ-019.

| # | Stage | Item | Why deferred | Resolution |
|---|---|---|---|---|
| C1 | 0 | Register positioning — what `cyclorama` claims and refuses, and the fork against `field-notes`, `orchard`, `instrument-console` | A genuine design call, and stage 3's gate is manual by design. The prior eighth-pack run deferred the same question as its C2 and never returned to it. | **Closed at stage 3.** Settled in the design record and carried into the pack's Register; T15 then proved the fork works in both directions. |
| C2 | 0 | The kit's signature components beyond the six spine ones | Depends on C1, and on what `DESIGN_SYNC_BRIDGE.md` lets across the border (motion does not cross). | **Closed at stage 3.** `FieldStop`, `AppWindow`, `StatusPill`, `ComparePanel`; `Eyebrow` deliberately left a class, not a component. |
| C3 | 0 | `docs/DOCMAP.md` → *Shared state* proved **overridden, not stale**: it requires a `git worktree` per run; the operator chose this checkout instead (D5). | The document lost to the operator, out loud. It is still correct as written for the concurrent case. | **Closed at stage 9.** The override is recorded beside the rule rather than deleting it: "no worktree" now has to be a decision taken against evidence, not a default taken from assumption. |
| C4 | 0 | The DOCMAP ratchet floors go stale the moment this run lands | The new floors are not known until stage 6 finishes. | **Closed at stage 9.** Rewritten to 875 / 305 / 192 with the commit they were measured at, per the file's own rule. Now 876 after this close-out pass. |
| C5 | 0 | The propagation matrix says a new pack needs "all nine headings"; the contract has been thirteen since 1.5.0 | Found during the harvest. Real staleness, unrelated to the override. | **Closed at stage 9** as REQ-018. The row now lists all eleven obligations, including the four it had omitted entirely — the `.cursor/` mirror, `install.sh`, the `.mdc` rule and the scenario. |
| C6 | 0 | `ADR-0001` is absent from `main`; the decision register begins at `0002` | Restored onto this run's branch as REQ-012, but `main` only gains it on merge. | **Closed at stage 7** on merge. The register now runs 0001, 0002. |
| C7 | 0 | Standing instruction 1's second half — re-check the tree immediately before `git add` | Cannot be discharged at stage 0; it is a pre-staging act by definition. | **Closed at stage 5**, and again before the close-out commit. No HEAD move, no foreign branch, every staged path this run's own. |
| C8 | 0 | `feat/lecture-hall-pack` remains an unmerged, held eighth-pack attempt | Deleting or merging another run's branch was not this run's call at stage 0. | **Closed 2026-08-08.** Its unique records were brought onto `main`; its `ADR-0001` already had been. Four already-merged `feat/*` branches were deleted and it followed, leaving **none** — so instruction 1's "a branch you did not create" is a real signal again instead of a standing list of five. Rule recorded in `DOCMAP.md`. |
| C9 | 6 | **`field-notes` contradicts itself about `--deep`** — the token layer annotates it "full-bleed dark band", the Palette table calls it "band", and Bans forbids "a dark band with a hard edge" below the hero. Surfaced by the T15b subagent, then reproduced against both files. | A **shipped** pack, and the fix read as a judgement call between two defensible readings rather than a typo. | **Closed 2026-08-08, and it was sharper than reported.** Checking the consumers settled it: **nothing consumes `--deep`, `--deep-2` or `--deep-3` anywhere in the bundle or the kits** — only the `-on-deep` family is used, and only inside `.fn-hero`, the dawn. So the annotation was wrong, not ambiguous. Corrected in the token layer, the Palette table and a new Gotchas entry; mirrored to `.cursor/`, kit token prefix regenerated byte-identical. |
| C10 | 7 | **`install.sh` documents an invocation that cannot work.** Its header says `Usage: ./install.sh [target-dir]`, but git recorded it `100644`, so `./install.sh` exits 126. CI and the README both use `sh install.sh`, which is why nobody noticed. | Pre-existing; not folded into the release, because re-cutting a verified tree for a comment costs more than the defect does. | **Closed 2026-08-08.** `git update-index --chmod=+x` → `100755`. `./install.sh` runs; the `sh install.sh` and `curl \| sh` paths are unaffected. |
| C11 | 9 | **The code graph is refreshed on its code half only.** `graphify . --update` **exited 0 while doing nothing** — it printed `error: no LLM API key found (35 doc files need semantic extraction)` and left `built_at_commit` three commits stale. Re-run with `--code-only`: 987 → **1042 nodes**, stamp now current, but the changed doc files were not re-extracted. Visible in `god-nodes`, where a hub still reads "T1–T14" though there are fifteen scenarios. `GRAPH_REPORT.md` not regenerated. | No backend key exists on this machine — checked `ANTHROPIC_API_KEY`, `GEMINI_API_KEY`, `GOOGLE_API_KEY`, `MOONSHOT_API_KEY`, `OPENAI_API_KEY`, `DEEPSEEK_API_KEY` and `~/.graphify`: all absent. Setting one is the operator's call and not a thing to improvise. | **OPEN — operator.** Set a backend key, then one full `graphify . --update`. Until then the next run's harvest should treat doc summaries as one release behind. The exit-0-on-error behaviour is worth reporting upstream: a refresh that reports success without refreshing is how a stale graph acquires the authority of a machine. It is also what standing instruction 9 now exists to catch. |
